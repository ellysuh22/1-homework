# macOS 가 자동 생성하는 메타데이터 파일
.DS_Store

# 터미널 실습 중 만들어지는 임시 폴더 (실습 로그는 logs/ 에 남으므로 추적 불필요)
practice/

# -----------------------------------------------------------------------------
#  개인 파일 — 공개 저장소에 절대 올라가면 안 된다.
#    미션 제약사항: "민감정보는 로그/이미지에 남기지 않는다"
#    실습과 무관한 개인 사진이므로 git add . 에도 걸리지 않도록 여기서 차단한다.
# -----------------------------------------------------------------------------
KakaoTalk_Photo_*.jpeg
KakaoTalk_Photo_*.jpg

# runlog.sh 연습용 로그 (제출용 로그 9개가 아니므로 추적 불필요)
logs/demo.log

# -----------------------------------------------------------------------------
#  주의: 위 demo.log 를 제외한 logs/*.log 와 evidence/*.png 는 절대 무시하면 안 된다.
#        미션이 요구하는 "수행 증거" 그 자체이므로 반드시 커밋되어야 한다.
# -----------------------------------------------------------------------------
