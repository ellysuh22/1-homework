# 핵심 카드 — 말하기용

> 구두 평가 직전에 이것만 봅니다. 읽는 문서가 아니라 **입으로 말하는 연습용**입니다.
> 자세한 건 [QNA.md](QNA.md), 쉬운 설명은 [EXPLAIN.md](EXPLAIN.md).

---

## ⭐ 모든 답을 이 틀에 넣는다

```
① 한 줄 답      →  ② 내가 한 실험      →  ③ 그래서 결론
   (10초)            (20초) ← 진짜 무기       (10초)
```

**정의를 외우지 말고 실험을 말한다.** 실험을 말하면 정의는 자동으로 따라 나온다.

막히면: **"제가 직접 해본 게 있는데요"** ← 이 한마디로 시작하면 말이 풀린다.

---

# 핵심 6개

## 1. 이미지 vs 컨테이너

| | |
|---|---|
| **한 줄** | 붕어빵 틀과 붕어빵입니다. 틀 하나로 붕어빵 여러 개를 찍어요. |
| **실험** | `my-web:1.0` 이미지 **하나**로 컨테이너 **세 개**를 동시에 띄웠습니다. 8080, 8081, 8082요. |
| **결론** | 이미지는 읽기 전용이라 안 바뀌고, 컨테이너는 여러 개 만들 수 있고 지워도 이미지는 남습니다. |
| **증거** | `logs/05-image-build.log`, `logs/06-port-mapping.log` |

**더 물으면**: 빌드는 `docker build`, 실행은 `docker run`. 컨테이너에서 파일을 고쳐도 이미지는 안 바뀝니다. → 그래서 볼륨이 필요합니다.

---

## 2. 포트 매핑

| | |
|---|---|
| **한 줄** | 컨테이너가 격리된 방이라서, `-p`로 다리를 안 놓으면 밖에서 못 닿습니다. |
| **실험** | 일부러 `-p` 없이 띄워봤습니다. 밖에서 `curl` 하니까 연결 실패가 나는데, 컨테이너 **안**에서 요청하니까 정상 응답이 왔습니다. |
| **결론** | 서버는 멀쩡히 돌고 있는데 다리가 없어서 못 닿는 거였습니다. `-p`가 그 다리입니다. |
| **증거** | `logs/06-port-mapping.log` |

**꼭 덧붙일 것 ⚠️**
> "`EXPOSE`는 다리가 아닙니다. '이 포트 씁니다'라고 적어둔 문서일 뿐이고, 실제로 여는 건 `-p`뿐입니다."

**충돌 얘기 나오면**
> "8080을 두 번 쓰니까 `port is already allocated`가 났습니다. 겹치면 안 되는 건 **호스트 포트**고, 컨테이너 안쪽 80번은 세 개가 다 같이 써도 격리돼 있어서 문제없습니다."

---

## 3. 절대 경로 vs 상대 경로

| | |
|---|---|
| **한 줄** | 절대 경로는 기준점이 **루트(`/`)로 고정**이고, 상대 경로는 기준점이 **지금 내 위치**입니다. |
| **실험** | 명령어를 글자 하나 안 바꾸고 폴더만 옮겨서 `cat ./hello.txt`를 쳤는데, 아까 읽히던 게 `No such file or directory`가 났습니다. |
| **결론** | 상대 경로는 내가 움직이면 기준점도 같이 움직입니다. 그래서 스크립트나 설정 파일에는 절대 경로를 씁니다. |
| **증거** | `logs/01-terminal.log` |

**킬러 한 마디 ⭐** (꼭 하세요)
> "그런데 그 실습을 하는 도중에 제가 똑같은 실수를 했습니다. 로그 파일 경로를 상대 경로로 줬는데, 스크립트가 `cd`로 들어가는 순간 로그가 통째로 끊겼습니다. 설명하려던 개념에 제가 당한 거죠. 그래서 `scripts/runlog.sh` 시작할 때 절대 경로로 정규화하도록 고쳤습니다."

---

## 4. 권한 755 / 644

| | |
|---|---|
| **한 줄** | `r=4`, `w=2`, `x=1`로 놓고 **가진 것만 더한** 값을 소유자·그룹·기타 순으로 나열한 겁니다. |
| **실험** | 폴더를 644로 만들어봤더니 `x`가 없어져서 **내 폴더인데 `Permission denied`로 못 들어갔습니다.** |
| **결론** | 폴더의 `x`는 '실행'이 아니라 **'진입'**입니다. 그래서 폴더는 755, 문서 파일은 644를 씁니다. |
| **증거** | `logs/02-permissions.log` |

**계산 빨리 하는 법**
```
rwx = 7      rw- = 6      r-x = 5      r-- = 4      --- = 0
```
`-`는 "그 권한 없음" → 0으로 치고 안 더한다.

**더 물으면**: `chmod 000`으로 만들면 **소유자여도** 못 읽습니다. root만 예외입니다.

---

## 5. 볼륨

| | |
|---|---|
| **한 줄** | 컨테이너를 지우면 안에 있던 데이터도 같이 사라집니다. 볼륨은 데이터를 컨테이너 **밖**에 두는 겁니다. |
| **실험** | 볼륨 쓴 경우와 안 쓴 경우를 **나란히** 해봤습니다. 볼륨 없이 하면 `No such file`이 나오는데, 볼륨을 쓰면 컨테이너를 완전히 지운 뒤에도 **기록 시각까지 그대로** 남아 있었습니다. |
| **결론** | 두 실험의 차이가 볼륨 하나뿐이니, 데이터를 지켜준 게 볼륨이라는 게 증명됩니다. |
| **증거** | `logs/08-volume.log` |

**바인드 마운트랑 뭐가 다르냐고 물으면**
> "둘 다 데이터를 밖에 두는 건데, 바인드 마운트는 **내가 정한 내 컴퓨터 폴더**를 쓰고 볼륨은 **도커가 관리하는 창고**를 씁니다. 개발할 땐 바인드 마운트, 운영에선 볼륨이요. 바인드 마운트는 `/Users/ellysuh/...` 같은 경로에 묶여서 다른 서버에 올리면 깨지거든요."

💡 구분법: `-v` 왼쪽이 `/`로 시작하면 바인드 마운트, 그냥 이름이면 볼륨.

---

## 6. Git vs GitHub

| | |
|---|---|
| **한 줄** | 깃은 **내 컴퓨터에 깔린 프로그램**이고, 깃허브는 그걸 올리는 **인터넷 사이트**입니다. |
| **실험** | 깃허브 업로드가 **두 번 연속 다른 이유로** 막혔습니다. 처음엔 다른 계정으로 로그인돼 있어서, 두 번째는 이메일 보호 설정 때문에요. |
| **결론** | 그런데 **로컬 깃 작업은 두 번 다 아무 문제가 없었습니다.** 막힌 건 전부 원격 쪽이었어요. 그래서 둘이 완전히 별개라는 걸 몸으로 알았습니다. |
| **증거** | `logs/09-git-github.log` |

**더 물으면**: 1차는 `gh api`로 권한을 먼저 조회해서 `push: false`인 걸 확인하고 재인증했습니다. 2차는 `GH007` 에러라 계정 설정을 바꿨고요.

---

## 7. SSH 키 (보너스)

| | |
|---|---|
| **한 줄** | 토큰은 **도장을 우편으로 부치는 것**, SSH는 **상대가 보낸 종이에 도장 찍어 종이만 돌려주는 것**입니다. 개인키는 제 컴퓨터를 안 떠나요. |
| **실험** | 키를 **못 쓰게 막고** 접속하니 `Permission denied (publickey)` 가 났고, 키를 쓰니까 `Hi ellysuh22!` 로 통과했습니다. remote도 `https://` → `git@` 로 바뀐 걸 전후로 남겼습니다. |
| **결론** | 차이가 **키 하나뿐**이라 문을 열어준 게 키라는 게 증명됩니다. 인증 수단이 토큰 전송에서 키 서명으로 바뀐 겁니다. |
| **증거** | `logs/10-ssh-key.log`, `evidence/github-ssh-key.png` |

**꼭 덧붙일 것 ⚠️**
> "`Hi` 뒤의 **계정명**을 봐야 합니다. 저장소 주인이랑 다르면 인증은 돼도 push가 막히거든요. 실제로 브라우저가 다른 계정으로 로그인돼 있어서 등록 직전에 잡았습니다."

**대조군 얘기 나오면** ⭐
> "키를 막았는데 인증이 **통과**해버렸습니다. `-v`로 보니 제가 `~/.ssh/config`에 넣은 `IdentityFile`이 살아 있었어요. `IdentitiesOnly`가 '설정 무시'가 아니라 '지정한 키만'이더군요. `-F /dev/null`로 설정 자체를 안 읽게 해서 해결했습니다."

**passphrase 물으면** ⚠️ (약점 — 미리 준비)
> "안 걸었습니다. 그래서 키 파일이 유출되면 그대로 쓰입니다. 대신 **권한 600, 저장소 밖 보관, 기기별 키 분리**로 관리했고, 유출되면 GitHub에서 **공개키 한 줄만 지우면 즉시 무효화**됩니다. 이 한계는 README 18-1절에 적어뒀습니다."

---

# 항목 1 — "했느냐" 질문용 (외울 것 없음)

**"네, 했습니다. ○○ 로그에 있습니다."** 로 끝난다.

| 물어보면 | 로그 파일 |
|---|---|
| 터미널 기본 명령 | `logs/01-terminal.log` |
| 권한 변경 | `logs/02-permissions.log` |
| 도커 설치 확인 | `logs/03-docker-check.log` |
| hello-world / 컨테이너 기본 | `logs/04-container-basics.log` |
| 이미지 빌드 | `logs/05-image-build.log` |
| 포트 매핑 | `logs/06-port-mapping.log` |
| 바인드 마운트 | `logs/07-bind-mount.log` |
| 볼륨 | `logs/08-volume.log` |
| 깃 / 깃허브 | `logs/09-git-github.log` |
| SSH 키 (보너스) | `logs/10-ssh-key.log` |

**스크린샷**: `evidence/` 폴더 (브라우저 8080·8081, VSCode 깃허브 계정, GitHub SSH 키 등록)

---

# 항목 4 — "겪은 일" 3개 (외우는 게 아니라 떠올리기)

| 질문 유형 | 꺼낼 이야기 |
|---|---|
| **가장 어려웠던 점** | 깃허브 2연속 거부 (계정 권한 → 이메일 정책) |
| **포트 충돌 진단 순서** | 에러 읽기 → `docker ps` → `lsof -i :8080` → **호스트 포트만** 변경 |
| **어이없었던 실수** | 상대 경로 때문에 로그 날아간 일 |
| **기술적으로 까다로웠던 것** | `docker run -it` 세션을 로그로 남기기 (`script` 명령 + `TERM=dumb`) |

---

# 막혔을 때 쓸 문장

**모를 때**
> "그건 확실히 모르겠습니다. 다만 관련해서 제가 확인한 건 ○○입니다."

**말이 꼬였을 때**
> "제가 직접 해본 걸로 말씀드리면요 —"

**설계 의도를 물을 때** (항목 2)
> "역할별로 나눴습니다. 이미지에 들어갈 것(`app/`, `templates/`), 실행할 때만 쓰는 것(`bindmount-demo/`), 증거로 남길 것(`logs/`, `evidence/`)으로요. 뒤의 것들은 `.dockerignore`로 제외했습니다."

**보너스 물으면** (항목 5)
> "보너스는 선택이라 다섯 개 중 **GitHub SSH 키**를 골라서 했습니다. 키 등록하고, 키를 막으면 거부되는 것까지 확인했습니다."

---

# ⚠️ 자주 틀리는 함정 (이것만 조심)

| 함정 | 정답 |
|---|---|
| `EXPOSE`가 포트를 연다 | ❌ 문서일 뿐. 여는 건 `-p` |
| 8080이 컨테이너다 | ❌ 컨테이너는 `web-8080`. 8080은 문 번호 |
| 폴더의 `x`는 실행 | ❌ **진입** |
| `docker --version`이면 확인 끝 | ❌ 그건 클라이언트만. 데몬은 `docker info`의 Server |
| 컨테이너 포트가 충돌한다 | ❌ 충돌하는 건 **호스트 포트** |

---

# 최종 30초 요약 (첫 질문이 "뭐 했어요?"일 때)

> "내 컴퓨터에서만 되는 게 아니라 **어디서든 똑같이 돌아가는 프로그램**을 만드는 연습이었습니다.
> 웹사이트 하나를 도커로 포장해서 이미지를 만들고, 그 이미지 하나로 컨테이너 세 개를 띄워서 브라우저로 확인했습니다.
> 그냥 '됐습니다'로 끝내지 않고, **안 되는 경우를 나란히 놓고 비교하는 방식**으로 증명했습니다. 포트 매핑 없이 띄워보고, 볼륨 없이 해보고, 상대 경로로 실패시켜보고요.
> 결과는 전부 로그와 스크린샷으로 남겨서 깃허브에 올렸습니다."
