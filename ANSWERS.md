# 평가문항 18개 — 실전 답변

> 평가표 순서 그대로입니다. **입으로 말하는 문장**으로 썼습니다.
> 각 문항: **답변** (그대로 말하면 됨) + **증거** (물으면 대는 위치) + **꼬리질문** (더 물으면)

**문서 3종 쓰는 법**
| 문서 | 언제 |
|---|---|
| [CARD.md](CARD.md) | 직전 5분. 핵심 6개만 |
| **ANSWERS.md** (이 문서) | **평가 준비. 18문항 실전 답변** |
| [QNA.md](QNA.md) | 더 깊게 파고들 때 |

---

# 항목 1 — 기능 동작 검증

> **"했느냐"를 묻는 항목.** 짧게 답하고 증거 위치만 대면 끝. 길게 말할 필요 없음.

## 1-1. 터미널에서 기본 명령어로 폴더/파일 생성·이동·삭제를 수행한 흔적이 있는가?

**답변**
> 네. 아홉 가지를 전부 했습니다. `pwd`, `ls -la`, `cd`, `mkdir`, `touch`, `cp`, `mv`, `rm`, `cat`입니다. 명령어랑 출력이 짝으로 로그에 남아 있습니다.

**증거** `logs/01-terminal.log`

**꼬리질문 — `mv`는 뭐가 특이한가요?**
> 같은 폴더 안에서 옮기면 이름 변경이 되고, 다른 폴더로 옮기면 이동이 됩니다. 컴퓨터 입장에선 같은 동작이라서요. 두 경우를 각각 실행해서 구분했습니다.

---

## 1-2. 파일 권한 변경 결과가 확인되는가?

**답변**
> 네. 파일은 644에서 600으로, 디렉토리는 755에서 700으로 바꾸고 `ls -l`을 전후로 비교했습니다. 그런데 표기만 바뀌는 걸 보이는 건 부족하다고 생각해서, **권한이 실제로 작동하는지까지** 확인했습니다. `chmod 000`으로 만든 뒤에 `cat`을 해서 소유자인데도 `Permission denied`가 나는 걸 기록했습니다.

**증거** `logs/02-permissions.log`

**꼬리질문 — 디렉토리는요?**
> `x` 권한을 빼고 `ls`를 시도해서 접근이 막히는 걸 보였습니다. 디렉토리의 `x`가 '실행'이 아니라 '진입' 권한이라는 걸 실패 출력으로 증명한 겁니다.

---

## 1-3. `docker --version`이 출력되고, Docker가 동작 가능한 상태인가?

**답변**
> 네. 버전은 29.4.0입니다. 그런데 `docker --version`은 **클라이언트가 설치됐는지만** 알려줍니다. 실제로 컨테이너를 돌리는 건 데몬이라서, 데몬이 죽어 있으면 버전은 나와도 아무것도 못 합니다. 그래서 `docker info`를 같이 실행해서 **Server 섹션이 정상 출력되는 것**까지 확인했습니다.

**증거** `logs/03-docker-check.log`

**꼬리질문 — macOS인데 어떻게 돌렸나요?**
> 도커 데몬은 리눅스 기술이라 맥에서 그냥은 안 돕니다. 학교 컴퓨터가 관리자 권한이 막혀 있어서 권한 없이도 되는 OrbStack을 썼습니다. `docker context ls`로 활성 컨텍스트가 `orbstack`인 것도 기록했습니다. 명령어는 완전히 동일합니다.

---

## 1-4. `docker run hello-world`가 정상 실행되는가?

**답변**
> 네. `Hello from Docker!`가 정상 출력됐습니다. 이 이미지는 메시지만 출력하고 바로 종료되는 설치 확인용인데, 출력 내용 자체가 도커의 동작 흐름을 설명해 줍니다 — 클라이언트가 데몬에 요청하고, 데몬이 이미지를 받아와서 컨테이너를 만들고, 결과를 다시 클라이언트로 돌려주는 순서요.

**증거** `logs/04-container-basics.log`

**꼬리질문 — 바로 종료되는데 어떻게 확인했나요?**
> `docker ps`에는 안 보이지만 `docker ps -a`에는 `Exited (0)`으로 기록이 남습니다. 그것도 같이 확인했습니다.

---

## 1-5. 이미지/컨테이너 목록 확인 및 정리 흔적이 있는가?

**답변**
> 네. `docker images`, `docker ps`, `docker ps -a`로 확인하고 `docker stop`, `docker rm`으로 정리했습니다. 운영 명령으로 `docker logs`, `docker stats --no-stream`도 수행했습니다.

**증거** `logs/03-docker-check.log`, `logs/04-container-basics.log`

**꼬리질문 — `docker pull`을 따로 한 이유는?**
> `docker run`만 해도 이미지가 자동으로 받아지는데, 그러면 '다운로드' 자체가 증거로 안 남습니다. 그래서 `pull`을 따로 실행해서 로그를 남겼습니다.

**꼬리질문 — `--no-stream`은 왜 붙였나요?**
> `docker stats`는 화면을 계속 갱신하는 실시간 모드라 로그 파일로 못 남깁니다. `--no-stream`을 붙이면 현재 시점 스냅샷 한 번만 출력하고 끝납니다.

---

## 1-6. Dockerfile로 이미지 빌드가 가능한가?

**답변**
> 네. `nginx:1.29-alpine`을 베이스로 커스텀 여섯 가지를 얹어서 `my-web:1.0`을 빌드했습니다. 메타데이터, 환경변수 분리, 정적 콘텐츠 교체, 서버 설정 교체, 포트 선언, 헬스체크입니다.
>
> 빌드가 성공했다는 것만으로는 부족하다고 생각해서, **`docker inspect`로 제가 적은 게 실제로 이미지에 박혔는지 하나씩 확인**했습니다. 헬스체크는 상태가 `starting`에서 `healthy`로 바뀌는 것까지 기록했습니다.

**증거** `Dockerfile` (지시어별 주석 포함), `logs/05-image-build.log`

**꼬리질문 — `--progress=plain`은 왜?**
> 기본 모드는 진행률 애니메이션이라 로그 파일에 제어문자가 잔뜩 남아서 못 읽습니다. `plain`을 쓰면 한 줄씩 찍혀서 사람이 읽을 수 있게 됩니다.

**꼬리질문 — 왜 태그를 `1.29`까지 고정했나요?**
> `nginx:alpine`처럼 버전 없는 태그는 그때그때 최신을 가리키는 움직이는 표적입니다. 오늘 제 컴퓨터와 다음 달 평가자 컴퓨터가 다른 버전을 받으면 결과가 달라지는데, 그건 미션이 요구하는 재현 가능한 환경과 어긋나서 마이너 버전까지 못 박았습니다.

---

## 1-7. 매핑된 포트로 접속이 가능한가?

**답변**
> 네. 8080과 8081 두 포트로 접속에 성공했습니다. 같은 이미지 하나로 컨테이너 두 개를 서로 다른 호스트 포트에 띄운 건데, 두 응답이 실제로 같은지 `shasum`으로 해시를 떠서 동일한 걸 확인했습니다. 미션이 주소창이 보이는 브라우저 화면을 요구해서 스크린샷도 첨부했습니다.

**증거** `logs/06-port-mapping.log`, `evidence/browser-8080.png`, `evidence/browser-8081.png`

---

## 1-8. Docker 볼륨 데이터가 컨테이너 삭제 후에도 유지되는가?

**답변**
> 네. 볼륨을 만들어서 파일을 기록하고, `docker rm -f`로 컨테이너를 완전히 지운 다음, 같은 볼륨을 새 컨테이너에 연결해서 읽었더니 **기록 시각까지 그대로** 나왔습니다.
>
> 여기서 하나 더 했습니다. **볼륨을 안 쓰는 대조군**으로 똑같은 절차를 밟아서 `No such file or directory`가 나오는 걸 나란히 기록했습니다. 두 실험의 차이가 볼륨 하나뿐이니까, 데이터를 지켜준 게 볼륨이라는 결론이 논리적으로 성립합니다.

**증거** `logs/08-volume.log`

---

## 1-9. Git 설정 및 GitHub 연동이 확인되는가?

**답변**
> 네. `user.name`, `user.email`, `init.defaultBranch main`을 설정하고 `git config --list` 전체 출력을 남겼습니다. 작업 단위를 나눠서 커밋했고 — 골격, 스크립트, 로그, 증거, 문서 순서로요 — `git push` 후에 `gh repo view`랑 GitHub API로 실제 파일 수까지 확인했습니다.
>
> VSCode는 계정 메뉴에 제 깃허브 계정이 표시된 스크린샷으로 로그인을 증명했고, `git branch -vv`로 로컬 브랜치가 원격을 추적하는 것도 CLI로 교차 확인했습니다.

**증거** `logs/09-git-github.log`, `evidence/vscode-github-account.png`

---

# 항목 2 — 동작 구조 설계

> 여기부터는 **"왜 그렇게 했는지"**를 묻는다. 판단 근거를 말해야 함.

## 2-1. 프로젝트 디렉토리 구조를 어떤 기준으로 구성했는지 설명할 수 있는가?

**답변 (한 줄)**
> **역할별로 나눴습니다.** 이미지에 들어갈 것, 실행할 때만 쓰는 것, 증거로 남길 것을 폴더로 분리했습니다.

**이어서**
> 세 갈래입니다. `app/`이랑 `templates/`는 이미지에 들어가고, `bindmount-demo/`는 실행 시점에 연결하는 거라 안 들어가고, `logs/`·`evidence/`·`scripts/`는 증거라서 제외했습니다. `.dockerignore`로 뺐습니다.

**이유 세 가지**

| | 이유 |
|---|---|
| ① | **이미지에 들어갈 것과 아닌 것을 명확히** 하려고. 로그나 스크린샷이 이미지에 섞이면 용량만 늘고 의미가 없습니다. |
| ② | **미션 문구에 맞췄습니다.** 미션이 "웹 서버 소스코드(예: `app/` 또는 `src/`)"라고 나열해서 흔히 쓰는 `site/` 대신 `app/`으로 뒀습니다. |
| ③ | **콘텐츠와 설정을 분리했습니다.** `app/`은 "무엇을 보여줄지", `templates/`는 "서버를 어떻게 굴릴지"입니다. |

**꼬리질문 — Dockerfile을 왜 루트에 뒀나요?**
> 하위 폴더로 옮기면 `docker build`에 경로 옵션을 따로 줘야 하고, 나중에 Compose를 붙일 때도 설정이 복잡해집니다.

---

## 2-2. 포트/볼륨 설정을 어떤 방식으로 재현 가능하게 정리했는지 설명할 수 있는가?

**답변 (한 줄)**
> **포트는 대역을 미리 나눠서 예약했고, 설정값은 환경변수로 빼서 명령 한 줄로 바꿀 수 있게 했습니다.** 그리고 모든 명령을 명령어+출력 짝으로 로그에 남겼습니다.

**① 포트 — 대역 예약**

| 포트 | 용도 |
|---|---|
| 8080 | 포트 매핑 1회차 |
| 8081 | 포트 매핑 2회차 |
| 8082 | 바인드 마운트 |
| 8090, 3000 | 보너스용 예약 (미사용) |

> 용도별로 미리 갈라두면 나중에 Compose를 띄워도 충돌이 안 납니다. 실제로 8080을 중복으로 써봤을 때 `port is already allocated`가 났기 때문에 미리 막아둔 겁니다.

**② 설정 — 환경변수로 분리**
> 포트를 설정 파일에 `listen 80`으로 박아두면 바꿀 때마다 파일 고치고 이미지를 다시 빌드해야 합니다. 그래서 `ARG/ENV APP_PORT`로 선언하고 nginx 설정을 **완성본이 아니라 템플릿**으로 넣었습니다. 컨테이너가 시작될 때 nginx 공식 이미지의 스크립트가 `${APP_PORT}`를 실제 값으로 치환해 줍니다. 덕분에 이미지는 그대로 두고 `docker run -e APP_PORT=3000` 한 줄로 포트를 바꿀 수 있습니다.

**③ 재현성 — 로그 수집 자체를 도구화**
> 미션에 "명령어 입력과 출력이 함께 포함되어야 한다"는 조항이 있는데, 손으로 복붙하면 빠뜨리기 쉽습니다. 그래서 `scripts/runlog.sh`에 헬퍼 함수를 만들어서 모든 실습을 그걸로 실행했습니다. 구조적으로 누락이 불가능합니다.
>
> 여기서 하나 더 신경 쓴 게, 처음엔 명령어를 그대로 기록했더니 공백이 든 인자의 따옴표가 사라져서 **로그를 보고 따라 치면 다른 명령이 되는** 문제가 있었습니다. 재현성이 깨지는 문제라 따옴표를 복원해서 기록하도록 고쳤습니다. 지금 로그는 복사해 붙이면 그대로 실행됩니다.

**증거** `scripts/runlog.sh`, `README.md` 재현 가이드

---

# 항목 3 — 핵심 기술 원리 적용

> **개념 4개.** 여기가 진짜 공부할 부분. 전부 "실험"을 같이 말할 것.

## 3-1. 이미지와 컨테이너의 차이를 "빌드/실행/변경" 관점에서 구분해 설명할 수 있는가?

**답변 (한 줄)**
> **이미지는 붕어빵 틀, 컨테이너는 붕어빵입니다.** 틀은 빌드로 한 번 만들고 안 바뀌고, 붕어빵은 실행할 때마다 새로 생기고 여러 개 만들 수 있습니다.

**세 관점으로 나누면**

| 관점 | 이미지 | 컨테이너 |
|---|---|---|
| **빌드** | `docker build`로 만든다. Dockerfile이 레시피 | 빌드로 만들어지지 않는다 |
| **실행** | 이미지 자체는 실행되지 않는다 | `docker run`으로 이미지에서 찍어낸다 |
| **변경** | **읽기 전용.** 고치려면 다시 빌드 | 쓰기 가능. 단 지우면 변경분도 사라짐 |

**실험**
> `my-web:1.0` 이미지 **하나**로 컨테이너 **세 개**를 동시에 띄웠습니다. 8080, 8081, 8082요. 하나를 지워도 이미지랑 다른 컨테이너는 멀쩡합니다.

**변경 관점 — 결정적 사례**
> 바인드 마운트 실습에서 컨테이너를 완전히 지웠다가 다시 띄웠는데도 화면에 `v2`가 나왔습니다. 그 내용이 이미지가 아니라 **호스트 폴더**에 있었기 때문입니다. **이미지에는 `v1`도 `v2`도 안 들어있습니다.**

**꼬리질문 — 층(layer)이 뭔가요?**
> Dockerfile 지시어 하나가 층 하나를 만듭니다. 층은 캐시되기 때문에 자주 바뀌는 내용을 뒤쪽에 둘수록 재빌드가 빨라집니다.

---

## 3-2. 컨테이너 내부 포트로 직접 접속할 수 없는 이유와, 포트 매핑이 필요한 이유를 설명할 수 있는가?

**답변 (한 줄)**
> **컨테이너가 격리된 공간이기 때문입니다.** 컨테이너는 자기만의 네트워크를 갖고 있어서, `-p`로 다리를 놓지 않으면 호스트에서 닿을 수 없습니다.

**이어서**
> 컨테이너 안의 `localhost`는 호스트의 `localhost`와 완전히 다른 겁니다. 그래서 컨테이너 안에서 80번이 열려 있어도 호스트에서는 안 보입니다. `-p 8080:80`이 "호스트 8080으로 오는 요청을 컨테이너 80으로 넘겨줘"라는 다리를 놓는 거고요.

**실험 — 일부러 다리를 안 놓아봄**
```
$ curl http://localhost:8082
Failed to connect                    ← 호스트에서는 못 닿음

$ docker exec web-noport wget -O - http://127.0.0.1:80/env
APP_ENV=dev                          ← 컨테이너 안에서는 정상 응답
```
> **서버는 멀쩡히 돌고 있는데 다리가 없어서 못 닿는 상황**을 그대로 재현한 겁니다.

**⚠️ 꼭 덧붙일 것 — `EXPOSE`는 다리가 아니다**
> `EXPOSE 80`은 이미지에 "이 포트를 씁니다"라고 적어둔 **문서**일 뿐입니다. 실제로 여는 건 `-p`뿐이고요. 위 실험에서 `docker ps`의 PORTS 칸에 `80/tcp`만 있고 `0.0.0.0:8082->` 같은 호스트 포트가 없었던 게 그 증거입니다.

**꼬리질문 — 세 컨테이너가 다 80번을 쓰는데 안 겹치나요?**
> 충돌하는 건 **호스트 포트**이지 컨테이너 포트가 아닙니다. 안쪽 80번은 서로 격리돼 있어서 문제없습니다.

**증거** `logs/06-port-mapping.log`

---

## 3-3. 절대 경로와 상대 경로를 어떤 상황에서 선택하는가?

**답변 (한 줄)**
> **위치가 바뀔 수 있으면 절대 경로, 같은 폴더 안에서 짧게 쓸 땐 상대 경로**를 씁니다. 스크립트나 설정 파일에는 절대 경로가 안전합니다.

**이유**
> 절대 경로는 기준점이 항상 루트(`/`)로 고정이고, 상대 경로는 기준점이 지금 있는 위치입니다. 그래서 **선택 기준은 "실행 위치가 바뀔 수 있는가"** 입니다.

| 절대 경로를 써야 할 때 | 상대 경로가 편한 때 |
|---|---|
| 스크립트 안, 설정 파일 | 터미널에서 손으로 칠 때 |
| 도커 볼륨 마운트 경로 | 프로젝트 안에서 이웃 파일 참조 |
| cron 같은 자동 실행 | 프로젝트를 통째로 옮겨도 유지됨 |

**실험**
> 명령어를 글자 하나 안 바꾸고 폴더만 옮겨서 `cat ./hello.txt`를 쳤는데, 아까 읽히던 게 `No such file or directory`가 났습니다.

**⭐ 킬러 — 실제로 겪은 사고 (꼭 말하기)**
> 그런데 그 실습을 하는 도중에 제가 똑같은 실수를 했습니다. 로그 파일 경로를 `logs/01-terminal.log`라는 상대 경로로 줬는데, 스크립트가 `cd`로 하위 폴더에 들어가는 순간 로그가 통째로 끊겼습니다.
>
> ```
> scripts/runlog.sh: line 38: logs/01-terminal.log: No such file or directory
> ```
>
> 공교롭게도 **그때 하고 있던 실습이 "절대 경로 vs 상대 경로 비교"** 였습니다. 설명하려던 개념을 제가 직접 당한 셈입니다. 해결은 스크립트 시작할 때 경로를 절대 경로로 정규화하는 거였습니다.

**꼬리질문 — 바인드 마운트랑도 관련 있나요?**
> 네. 바인드 마운트에서 호스트 경로에 반드시 절대 경로를 써야 하는 것도 같은 이유입니다. 그래서 `$(pwd)`로 현재 위치를 절대 경로로 바꿔 넣었습니다.

---

## 3-4. 파일 권한 숫자 표기(755, 644)가 어떤 규칙으로 결정되는가?

**답변 (한 줄)**
> **`r=4`, `w=2`, `x=1`로 놓고 가진 것만 더한 값**을 소유자·그룹·기타 순서로 나열한 겁니다.

**계산**
```
rwx = 4+2+1 = 7      rw- = 4+2 = 6      r-x = 4+1 = 5      r-- = 4      --- = 0

755 = rwx r-x r-x          644 = rw- r-- r--
      소유자 그룹 기타            소유자 그룹 기타
```

**왜 폴더는 755, 파일은 644인가**
> 핵심은 **폴더의 `x`가 '실행'이 아니라 '진입'** 이라는 점입니다. 폴더에 `x`가 없으면 그 안에 들어갈 수 없습니다. 그래서 폴더는 `x`가 필요해서 755, 일반 문서는 실행할 일이 없으니 `x`를 빼고 644를 씁니다.

**실험**
```
$ chmod 644 practice/perm-demo      # 폴더에서 x 제거
$ ls practice/perm-demo
Permission denied                    ← 내 폴더인데 못 들어감
```

**꼬리질문 — 소유자면 다 되는 거 아닌가요?**
> 아닙니다. `chmod 000`으로 만들면 **소유자여도** 못 읽습니다. 소유자라고 무조건 통과하는 게 아니라 권한 비트를 그대로 따릅니다. root만 예외입니다.

---

# 항목 4 — 심층 인터뷰

> **가장 중요한 항목.** 실제로 겪은 일을 말해야 설득력이 있음.

## 4-1. "호스트 포트가 이미 사용 중"이라 포트 매핑이 실패한다면, 어떤 순서로 원인을 진단하겠는가?

**답변 (한 줄)**
> **에러 메시지 확인 → 누가 그 포트를 쓰는지 조회 → 도커 안/밖 구분 → 포트 변경 또는 기존 컨테이너 정리** 순서입니다. 실제로 겪어봤습니다.

**실제 겪은 상황**
```
$ docker run -d -p 8080:80 --name web-conflict my-web:1.0
docker: Error response from daemon: failed to set up container networking:
Bind for 0.0.0.0:8080 failed: port is already allocated
[exit code: 125]
```

**진단 순서 5단계**

**1단계 — 에러 메시지를 정확히 읽는다**
> `port is already allocated` → 8080을 이미 누가 잡고 있다는 뜻입니다. 종료 코드 125는 "도커가 컨테이너를 시작하기도 전에 실패했다"는 의미고요.

**2단계 — 도커 컨테이너 중에 범인이 있는지 본다**
```bash
docker ps --format 'table {{.Names}}\t{{.Ports}}'
```
> 저는 여기서 `web-8080`이 `0.0.0.0:8080->80/tcp`를 잡고 있는 걸 확인했습니다.

**3단계 — 도커 밖의 프로세스일 가능성도 확인한다**
```bash
lsof -i :8080
```
> `docker ps`에 안 보이면 도커가 아닌 다른 프로그램이 쓰는 겁니다.

**4단계 — 조치를 선택한다**

| 상황 | 조치 |
|---|---|
| 기존 컨테이너가 필요 없음 | `docker rm -f web-8080` 후 재실행 |
| 기존 것도 살려야 함 | **새 컨테이너의 호스트 포트만 변경** (`-p 8081:80`) |
| 도커 밖 프로그램이 점유 | 그 프로그램을 끄거나 다른 포트 사용 |

> 저는 두 번째를 택해서 8081을 썼습니다.

**5단계 — 뒤처리를 잊지 않는다**
> 실패한 컨테이너가 `Created` 상태로 남아 있어서 `docker rm -f`로 정리해야 합니다. 안 그러면 같은 이름으로 다시 만들 때 이름 충돌이 납니다.

**💡 핵심 포인트**
> 충돌하는 건 **호스트 포트**이지 컨테이너 포트가 아닙니다. `-p 8081:80`처럼 왼쪽만 바꾸면 됩니다. 이걸 모르면 컨테이너 포트까지 바꾸려다 설정이 꼬입니다.

**증거** `logs/06-port-mapping.log`

---

## 4-2. 컨테이너 삭제 후 데이터가 사라진 경험이 있다면, 이를 방지하기 위한 대안을 설명할 수 있는가?

**답변 (한 줄)**
> 네. **일부러 재현해봤습니다.** 대안은 볼륨이고, 볼륨을 쓴 경우와 안 쓴 경우를 나란히 실험해서 비교했습니다.

**왜 사라지는가**
> 컨테이너에 쓴 파일은 컨테이너 전용 저장 층에 저장됩니다. 컨테이너를 지우면 그 층도 같이 삭제되니까 데이터도 사라집니다. 이미지는 읽기 전용이라 변경분을 흡수하지 않고요.

**재현한 결과**
```
$ docker exec novol-test bash -c 'echo "데이터" > /data-inside.txt'
$ docker rm -f novol-test
$ docker exec novol-test2 cat /data-inside.txt
cat: /data-inside.txt: No such file or directory      ← 사라짐
```

**대안 1 — 볼륨 (운영에 적합)**
```
$ docker volume create my-data
$ docker run -d --name vol-test -v my-data:/data ...      # 연결해서 기록
$ docker rm -f vol-test                                    # 컨테이너 삭제
$ docker run -d --name vol-test2 -v my-data:/data ...     # 같은 볼륨 재연결
$ docker exec vol-test2 cat /data/hello.txt
기록 시각: 2026-07-31 11:42:47                             ← 시각까지 그대로
```

**대안 2 — 바인드 마운트 (개발에 적합)**
> 호스트 폴더를 직접 연결합니다. 제 컴퓨터 파일이라 컨테이너와 무관하게 남습니다.

**둘 중 뭘 쓰나**

| | 바인드 마운트 | 볼륨 |
|---|---|---|
| 저장 위치 | 내가 지정한 호스트 폴더 | 도커가 관리 |
| 적합한 곳 | 개발 | 운영 (DB 데이터) |
| 이식성 | 호스트 경로 의존 → 다른 PC에서 깨짐 | 이름으로 관리 → 이식성 좋음 |

> **운영이라면 볼륨을 씁니다.** 바인드 마운트는 `/Users/ellysuh/...` 같은 경로에 묶여 있어서 다른 서버에 올리면 깨지니까요.

**💡 강조할 점**
> 저는 "볼륨을 쓰면 됩니다"에서 그치지 않고 **대조 실험**을 했습니다. 성공 사례만 보이면 "볼륨 덕분"인지 "원래 안 지워지는 건지" 구분이 안 됩니다. 볼륨 없는 경우를 나란히 놓아야 인과가 증명됩니다.

**증거** `logs/08-volume.log`

---

## 4-3. 이 미션에서 가장 어려웠던 지점과 해결 과정(가설 → 확인 → 조치)을 근거와 함께 설명할 수 있는가?

**답변 (한 줄)**
> **GitHub 업로드가 두 번 연속 다른 이유로 막힌 것**이 가장 어려웠습니다. 로컬 작업은 멀쩡한데 원격에서만 막혀서, Git과 GitHub이 별개라는 걸 몸으로 배웠습니다.

### 1차 — 계정 권한 문제

**문제**
> 로컬 원격 주소는 `ellysuh22/1-homework`인데, GitHub CLI는 다른 계정으로 로그인돼 있었습니다.

**가설**
> 계정이 두 개인데 인증된 계정이 저장소 주인이 아니라서 쓰기 권한이 없을 것이다.

**확인** — push를 시도해 실패를 보기 전에, API로 권한을 직접 조회
```
$ gh api repos/ellysuh22/1-homework --jq '.permissions'
{"admin":false,"maintain":false,"pull":true,"push":false,...}
```
> `push: false` — 가설이 사실로 확인됐습니다.

**조치**
> 저장소 주인 계정으로 재인증했습니다. 새 저장소를 만들어 remote를 바꾸는 방법도 있었지만, 이미 만들어둔 제출용 저장소를 그대로 쓰려고 재인증을 택했습니다.

### 2차 — 이메일 프라이버시 정책

**문제** — 권한을 해결하고 push했더니 **또** 거부
```
remote: error: GH007: Your push would publish a private email address.
! [remote rejected] main -> main (push declined due to email privacy restrictions)
```

**가설**
> 이번엔 권한이 아니라 정책 문제로 보였습니다. 커밋 작성자 이메일이 실제 주소인데, 계정에 "이메일을 노출하는 커맨드라인 push 차단" 옵션이 켜져 있어서 GitHub이 막아준 것으로 추정했습니다.

**확인**
```
$ git log -1 --format='%an <%ae>'
ellysuh22 <실제 이메일>        ← 실제 이메일이 들어있음
```

**조치**
> 선택지가 둘이었습니다. noreply 이메일로 바꾸고 히스토리를 재작성하거나, 차단 옵션을 해제하거나. 두 번째를 택해서 해결했습니다.

### 배운 것 3가지

| | 내용 |
|---|---|
| ① | **Git과 GitHub은 완전히 별개입니다.** 두 번 다 로컬 Git에는 아무 문제가 없었습니다. 막힌 건 전부 원격 쪽 — 권한과 정책이었습니다. |
| ② | **커밋 이메일은 시작 전에 정해야 합니다.** 커밋을 만든 뒤에 바꾸려면 히스토리 재작성이 필요합니다. |
| ③ | **에러를 만나기 전에 확인하는 습관이 생겼습니다.** 무작정 push했으면 원인 파악에 시간이 더 걸렸을 텐데, `gh api`로 권한을 먼저 조회하니 한 번에 확인됐습니다. |

### 💬 다른 각도로 물으면

**"기술적으로 가장 까다로웠던 건?"**
> `docker run -it`으로 컨테이너에 들어간 세션을 로그로 남기는 것이었습니다. `-it`은 가상 터미널을 요구해서 일반적인 방법으로는 파일에 안 담깁니다. macOS 내장 `script` 명령으로 가상 터미널을 할당하고, 컨테이너 안에서 색상 출력을 끄도록(`TERM=dumb`) 설정해서 프롬프트가 그대로 남은 깨끗한 로그를 얻었습니다.

**"어이없었던 실수는?"**
> 로그 파일 경로를 상대 경로로 써서 `cd` 이후 로그가 통째로 날아간 일입니다. 그때 하고 있던 실습이 하필 "절대 경로 vs 상대 경로 비교"였습니다. 설명하려던 개념을 제가 직접 당해서 확실히 기억에 남았습니다.

---

# 항목 5 — 보너스 평가 포인트

## 5-1. 보너스 과제를 수행했는가?

**답변 (한 줄)**
> 이번 제출에는 포함하지 않았습니다. 다만 **나중에 추가할 때 기존 산출물을 하나도 고치지 않아도 되도록 미리 설계해 뒀습니다.**

**이어서**
> 보너스 다섯 항목이 기존 결과물에 어떤 영향을 주는지 먼저 따져봤는데, **결합점이 딱 하나 있었습니다 — 환경 변수로 포트 변경 항목**입니다.
>
> 만약 nginx 설정에 `listen 80`을 그대로 뒀다면, 나중에 이 항목을 하려고 Dockerfile을 고치고, README의 Dockerfile 전문을 고치고, 빌드 로그까지 다시 찍어야 했을 겁니다. 그래서 기본 과제 단계에서 미리 `ARG/ENV APP_PORT` + nginx 템플릿 구조로 만들었습니다. 이제 보너스는 명령 한 줄이면 끝나고 **파일 수정이 0**입니다.

```bash
docker run -d -p 3000:3000 -e APP_PORT=3000 -e APP_ENV=prod my-web:1.0
```

**낭비 아니냐고 물으면**
> 미션 (A) 방식이 요구하는 "정적 콘텐츠/**설정**만 교체"에서 '설정 교체'에 정확히 해당합니다. 기본 과제 요구도 동시에 만족합니다.

**추가로 준비해둔 것**
> 포트 대역을 예약해서(8090, 3000 비워둠) 나중에 Compose를 띄워도 충돌하지 않고, README 체크리스트를 필수/보너스로 물리적으로 분리해서 수정 지점이 체크박스 다섯 줄로 국한됩니다. SSH 전환에 대비해 HTTPS 시절의 `git remote -v` 출력도 지우지 않고 남겨뒀습니다.

---

# 막혔을 때

**모를 때**
> "그건 확실히 모르겠습니다. 다만 관련해서 제가 확인한 건 ○○입니다."

**말이 꼬였을 때**
> "제가 직접 해본 걸로 말씀드리면요 —"

---

> 📄 핵심 6개만: [CARD.md](CARD.md) · 명령어 원본: [README.md](README.md) · 쉬운 개념 설명: [EXPLAIN.md](EXPLAIN.md)
