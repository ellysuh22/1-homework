# 수행 보고서 — 개발 워크스테이션 구축

> 이 문서는 평가자를 위한 설명서입니다. [README.md](README.md)가 명령어와 출력을 그대로 담은 **증거 문서**라면, 이 문서는 그 증거들이 어떤 판단에서 나왔고 무엇을 보여주려 한 것인지를 설명하는 **해설 문서**입니다. 저장소를 처음 여시는 분이 이 글을 먼저 읽으면 전체 그림을 잡으실 수 있도록 썼습니다.

---

## 1. 이 과제를 어떻게 이해했는가

미션 문서를 처음 읽고 가장 먼저 한 일은 요구사항을 전부 뽑아내는 것이었습니다. 그런데 읽다 보니 요구가 한곳에 모여 있지 않았습니다. 2장 「최종 결과물」, 4장 「기능 요구 사항」, 7장 「제약 사항」에 요구가 흩어져 있고, 세 곳의 내용이 겹치면서도 미묘하게 달랐습니다.

예를 들어 "트러블슈팅 2건 이상"과 "웹 서버 소스코드", "바인드 마운트", "VSCode 연동"은 2장에만 있고 4장에는 없습니다. 반대로 "터미널 9종 조작", "권한 실습", "`docker pull`과 `docker stop`", "코드블록으로 정리" 같은 요구는 4장에만 있습니다. 7장에만 있는 것도 있습니다 — "명령어 입력과 출력 결과를 함께 기록", "모든 작업은 CLI 기반", "재현성", "개인 PC 종속 경로에 대한 대체 방법 병기".

그래서 한 장만 읽고 작업했다면 통째로 빠뜨렸을 항목이 여럿 있었습니다. 세 장의 **합집합**을 기준으로 총 53개 요구사항을 뽑아 체크리스트로 만든 뒤, 마지막에 한 줄씩 다시 대조하는 방식으로 작업했습니다.

특히 3장 「과제 목표」의 여섯 항목은 해석이 갈릴 수 있는 부분이었습니다. "스스로 설명할 수 있어야 한다"고만 되어 있고 문서에 쓰라는 말은 없습니다. 하지만 제출물이 저장소 링크뿐이고 "기술 문서만 읽어도 전체 수행 내용을 파악할 수 있어야 한다"고 못 박혀 있으니, 설명 능력을 증명할 경로는 README밖에 없다고 판단했습니다. 그래서 README 15장에 여섯 항목을 각각 독립 소제목으로 배치하고, 각 설명이 실제 실습 로그의 어느 부분을 근거로 하는지 서로 링크해 두었습니다.

---

## 2. 무엇을 만들었는가

결과물은 `nginx:1.29-alpine`을 베이스로 직접 작성한 Dockerfile과, 그것으로 빌드한 `my-web:1.0` 이미지, 그리고 그 이미지를 여러 방식으로 실행하며 남긴 아홉 개의 로그 파일과 네 장의 스크린샷입니다. 커밋은 골격·스크립트·로그·증거·문서 단위로 나누어 작업 흐름이 이력에 드러나도록 했습니다.

디렉토리 구성에서 한 가지 의도적으로 맞춘 부분이 있습니다. 미션 2장은 "웹 서버 소스코드(예: `app/` 또는 `src/`)"를 Dockerfile과 **별개 산출물**로 나열합니다. 그래서 정적 콘텐츠 폴더 이름을 흔히 쓰는 `site/`가 아니라 미션 문구 그대로 `app/`으로 두었습니다. 서버 설정은 `templates/`에 따로 분리했습니다.

실습 스크립트를 `scripts/`에 함께 커밋한 것도 의도적입니다. 로그만 올려두면 "정말 이 명령을 실행했는가"를 평가자가 확인할 방법이 없습니다. 실행한 스크립트 자체를 함께 두면 로그가 어떻게 만들어졌는지 검증할 수 있고, 그대로 다시 돌려 같은 로그를 재생성할 수도 있습니다.

---

## 3. 왜 이렇게 만들었는가

### 방식 (A)를 택한 이유

미션은 웹서버 베이스 이미지에 콘텐츠·설정을 얹는 (A)와, 리눅스 베이스에 패키지·사용자·환경변수·헬스체크를 추가하는 (B) 중 하나를 고르라고 합니다. (A)를 택했습니다.

가장 큰 이유는 검증의 선명함입니다. (A)는 웹서버가 이미 들어 있어서 포트 매핑과 바인드 마운트를 곧바로 시연할 수 있습니다. 특히 바인드 마운트는 nginx가 요청이 올 때마다 파일을 다시 읽기 때문에, 호스트에서 파일을 고치는 즉시 응답이 바뀌는 것을 재시작 없이 보여줄 수 있습니다. (B)로 갔다면 서버를 직접 띄우는 과정에서 포그라운드 프로세스 유지, 권한, 패키지 설치 실패 같은 곁가지 문제에 시간을 쓰게 됐을 것입니다.

다만 (A)를 택하면서 (B)의 장점인 환경변수와 헬스체크는 그대로 가져왔습니다. 그래서 커스텀 포인트가 여섯 개가 되어 설명할 거리가 (B)에 밀리지 않으면서, 빌드 실패 위험은 (A) 수준으로 낮게 유지했습니다.

한 가지 **일부러 하지 않은 것**도 밝혀둡니다. `USER` 지시어로 비특권 사용자 전환을 하지 않았습니다. 비특권 사용자는 1024번 미만 포트를 바인드할 수 없어서 80번을 쓰려면 포트를 1024 이상으로 바꾸고 `/var/cache/nginx` 권한까지 조정해야 합니다. 이번 과제의 초점에서 벗어난다고 판단해 넣지 않았습니다.

### 베이스 이미지 버전을 고정한 이유

`nginx:alpine`이 아니라 `nginx:1.29-alpine`으로 마이너 버전까지 못 박았습니다. `alpine` 같은 움직이는 태그는 시간이 지나면 다른 버전을 가리키기 때문에, 평가자가 나중에 빌드했을 때 제 결과와 달라질 수 있습니다. 미션이 반복해서 강조하는 "재현 가능한 환경"과 정면으로 어긋납니다. 태그를 고정하면 언제 빌드해도 같은 이미지가 나옵니다.

### 설정을 환경변수로 뺀 이유

Dockerfile에서 `ARG/ENV APP_PORT`를 선언하고, nginx 설정을 완성된 `.conf`가 아니라 `.template`으로 넣었습니다. nginx 공식 이미지의 엔트리포인트가 컨테이너 시작 시 `envsubst`로 `${APP_PORT}`를 실제 값으로 치환해 설정 파일을 생성해 줍니다.

이렇게 한 데는 두 가지 이유가 있습니다. 첫째, 미션이 (A) 방식에 요구하는 "정적 콘텐츠**/설정**만 교체"에서 '설정 교체'에 정확히 해당합니다. 원본 이미지의 `default.conf`는 `listen 80`이 하드코딩되어 있어 손댈 수 없는데, 템플릿으로 대체하면 그 부분을 바깥에서 제어할 수 있게 됩니다.

둘째는 보너스 과제를 염두에 둔 것입니다. 보너스에 "환경 변수를 주입해 서버 포트/모드를 바꿔본다"는 항목이 있는데, 만약 `listen 80`을 그대로 뒀다면 나중에 이 항목을 하려고 Dockerfile을 고치고, README의 Dockerfile 전문을 고치고, 빌드 로그까지 다시 찍어야 했을 것입니다. 지금 구조에서는 `docker run -e APP_PORT=3000` 한 줄이면 되고 **기존 파일은 하나도 건드리지 않습니다.** 포트 대역도 기본 과제는 8080·8081·8082만 쓰고 8090과 3000을 비워두어, 나중에 Compose를 띄워도 충돌하지 않게 했습니다.

### 로그 수집 도구를 따로 만든 이유

미션 7장에 "캡처/로그에는 명령어 입력과 출력 결과가 함께 포함되어야 한다"는 조항이 있습니다. 짧은 한 줄이지만 사실상 모든 증거 항목에 걸리는 요구입니다. 손으로 복붙하다 보면 출력만 붙이고 명령어를 빠뜨리기 쉬운데, 그러면 항목 하나가 아니라 전체가 감점 대상이 됩니다.

그래서 `scripts/runlog.sh`에 `logrun`이라는 헬퍼를 만들어 모든 실습을 이 함수로 실행했습니다. 명령어 한 줄과 그 출력이 반드시 짝으로 기록되기 때문에 구조적으로 누락이 불가능합니다.

여기에 한 가지 더 신경 쓴 부분이 있습니다. 처음에는 명령어를 `"$*"`로 기록했는데, 이렇게 하면 공백이 들어간 인자의 따옴표가 사라져서 로그를 보고 그대로 따라 쳤을 때 다른 명령이 되어버립니다. 예를 들어 `docker ps --format 'table {{.Names}}\t{{.Status}}'`가 따옴표 없이 기록되는 식입니다. 재현성이 깨지는 문제라, 인자에 특수문자가 있으면 따옴표를 복원해 기록하도록 고쳤습니다. 지금 로그는 그대로 복사해 붙이면 같은 명령이 실행됩니다.

---

## 4. 어떻게 검증했는가

이번 과제에서 가장 신경 쓴 부분입니다. 명령을 실행해 성공 로그를 남기는 것만으로는 "왜 이 설계가 필요한가"를 보여주지 못한다고 생각했습니다. 그래서 주요 항목마다 **대조군을 함께 두는 방식**을 택했습니다.

**볼륨**이 대표적입니다. 볼륨을 붙인 컨테이너에 파일을 쓰고 컨테이너를 지운 뒤 새 컨테이너에서 읽어 데이터가 살아있는 것을 보이는 것까지가 미션의 요구입니다. 여기에 더해, 볼륨을 **붙이지 않은** 컨테이너로 완전히 같은 절차를 밟아 데이터가 사라지는 것을 나란히 기록했습니다. 두 실험의 유일한 차이가 볼륨 연결 여부이므로, 데이터를 지켜준 것이 볼륨이라는 결론이 논리적으로 성립합니다. 볼륨 쪽에서는 `기록 시각: 2026-07-31 11:42:47`이라는 타임스탬프가 컨테이너 삭제 후에도 그대로 나왔고, 대조군에서는 `No such file or directory`가 나왔습니다.

**포트 매핑**도 같은 방식입니다. `-p 8080:80`으로 접속에 성공하는 것만 보이면 "포트 매핑이 왜 필요한가"에 대한 답이 되지 않습니다. 그래서 `-p` 없이 컨테이너를 띄워 호스트에서 `curl`이 실패하는 것(`Failed to connect`)과, 동시에 컨테이너 **안에서는** `wget`이 정상 응답하는 것을 함께 보였습니다. 서버는 멀쩡히 돌고 있는데 다리가 없어서 닿지 못하는 상황을 그대로 만든 것입니다. 덤으로 `EXPOSE 80`이 이미지에 선언되어 있는데도 `docker ps`의 PORTS 칸에 호스트 포트가 없는 것을 보여, `EXPOSE`는 문서일 뿐 실제 공개는 `-p`가 한다는 점도 확인했습니다.

**권한**도 표기만 바꾸고 끝내지 않았습니다. `chmod 600`으로 전후 표기가 달라지는 것을 보인 다음, `chmod 000`으로 만들고 `cat`을 실행해 소유자인데도 `Permission denied`가 나는 것을 기록했습니다. 디렉토리에서는 `x` 권한을 빼고 `ls`를 시도해 접근이 막히는 것을 보였습니다. 디렉토리의 `x`가 '실행'이 아니라 '진입' 권한이라는 것을 말로 설명하는 대신 실패 출력으로 증명한 셈입니다.

**attach와 exec의 차이**는 미션이 "스스로 관찰하고 정리하라"고 요구한 항목입니다. 1초마다 숫자를 출력하는 컨테이너를 띄워두고 세 가지 방법으로 접근했습니다. `docker logs`로는 밖에서 출력만 보고, `docker exec`로 들어가 명령을 실행한 뒤 나와도 컨테이너는 `Up` 상태를 유지했습니다. 반면 `docker attach`로 붙은 뒤 `Ctrl+C`를 누르자 컨테이너가 `Exited (130)`으로 종료됐습니다. 130이라는 숫자는 `128 + 2(SIGINT)`로, Ctrl+C 신호가 메인 프로세스까지 전달됐다는 뜻입니다. exec은 새 문을 내고 들어가는 것이고 attach는 이미 열린 문으로 들어가 같은 화면을 보는 것이라는 차이가 종료 코드 하나에 그대로 드러납니다.

**ubuntu 컨테이너 진입**은 기술적으로 까다로운 부분이었습니다. `docker run -it`은 가상 터미널을 요구해서 보통의 방법으로는 로그 파일에 담기지 않습니다. macOS 내장 `script` 명령으로 가상 터미널을 할당하고, 컨테이너 안에서 색상 출력을 끄도록(`TERM=dumb`) 설정한 뒤 세션을 기록했습니다. 그 결과 `bash-5.2#`라는 **컨테이너 내부 프롬프트**가 그대로 남은 로그를 얻었습니다. 밖에서 `docker exec`로 명령만 실행한 것이 아니라 실제로 안에 들어갔다는 증거가 됩니다.

---

## 5. 겪은 문제들

미션은 트러블슈팅 2건 이상을 요구하는데, 실제로 다섯 건을 겪었습니다. 지어낸 것이 아니라 작업 중 실제로 막혔던 지점들이고, 각각을 "문제 → 원인 가설 → 확인 → 해결" 형식으로 README 16장에 정리했습니다. 여기서는 그중 특히 배운 것이 있었던 두 건만 이야기하겠습니다.

첫째는 **상대 경로 때문에 로그가 통째로 사라진 일**입니다. 로그 파일 경로를 `logs/01-terminal.log`라는 상대 경로로 지정했는데, 스크립트가 `cd`로 하위 폴더에 들어간 순간부터 `No such file or directory`가 쏟아지며 기록이 끊겼습니다. 공교롭게도 그때 하고 있던 실습이 "절대 경로 vs 상대 경로 비교"였습니다. 설명하려던 개념을 스스로 실수로 재현한 셈입니다. 스크립트 시작 시 로그 경로를 절대 경로로 정규화하도록 고쳐 해결했고, 이 경험을 개념 설명 항목과 서로 링크해 두었습니다.

둘째는 **GitHub에서 두 번 막힌 일**입니다. 처음에는 로컬 원격이 `ellysuh22/1-homework`를 가리키는데 CLI는 다른 계정으로 로그인되어 있어 권한이 없었습니다. push를 시도해 실패를 보기 전에 `gh api repos/... --jq .permissions`로 조회해 `"push": false`를 먼저 확인했습니다. 계정을 전환해 해결했더니, 이번에는 `GH007` 에러로 다시 거부됐습니다. 커밋에 실제 이메일이 들어 있는데 계정에 "이메일을 노출하는 커맨드라인 push 차단" 옵션이 켜져 있었던 것입니다. GitHub이 사용자를 대신 보호해 준 것이었습니다.

이 두 건은 함께 놓고 보면 Git과 GitHub의 역할 차이를 잘 보여줍니다. 로컬 Git 설정에는 아무 문제가 없었고 커밋도 정상적으로 만들어졌습니다. 막힌 것은 전부 원격 쪽 — 권한과 정책이었습니다. 로컬 버전관리와 원격 협업 플랫폼이 별개라는 것을 실패로 체감한 사례라, 개념 설명 항목에도 이 경험을 근거로 달아두었습니다.

---

## 6. 확인하시는 방법

가장 빠른 검증 경로는 README 4장의 표입니다. 항목마다 검증 명령, 기대 결과, 증거 파일 링크를 한 줄로 묶어두었으니 관심 있는 항목부터 바로 확인하실 수 있습니다.

직접 실행해 보시려면 README 17장의 재현 절차를 따라 주십시오. 저장소를 clone하고 `docker build -t my-web:1.0 .` 후 컨테이너를 띄우면 동일한 결과가 나옵니다. 호스트 경로에 의존하는 부분이 없도록 스크립트는 모두 자기 위치를 기준으로 동작하게 작성했고, 8080 포트가 이미 사용 중이라면 호스트 쪽 숫자만 바꾸시면 됩니다. macOS 외 환경에서 달라지는 부분(`sed -i` 문법, `script` 명령 옵션 등)도 17장 표에 대체 방법을 함께 적어두었습니다.

로그 파일에 대해 한 가지 미리 말씀드릴 것이 있습니다. 각 로그는 그 단계를 수행한 시점의 스냅샷입니다. 뒤 단계로 갈수록 이미지와 컨테이너가 늘어나기 때문에, 뒤쪽 로그의 `docker images`나 `docker ps` 결과가 앞쪽과 다릅니다. 앞뒤가 맞지 않는 것이 아니라 시간 순서에 따른 자연스러운 변화입니다.

민감정보는 커밋 전에 토큰·개인키·비밀번호 패턴으로 전체 파일과 스테이지를 검색해 0건을 확인했고, 스크린샷 네 장도 모두 직접 열어 육안으로 점검했습니다. 로그에 보이는 `gho_****`는 GitHub CLI가 자체적으로 마스킹한 표기입니다.

---

## 7. 남은 것

보너스 과제는 이번 제출에 포함하지 않았습니다. 다만 나중에 추가할 때 기존 산출물을 뜯어고치지 않아도 되도록 설계해 두었습니다. Compose는 `docker-compose.yml`을 새로 만들면서 기존 Dockerfile을 `build: .`로 재사용하면 되고, 환경변수 항목은 앞서 설명한 템플릿 구조 덕분에 실행 명령 한 줄로 끝납니다. SSH 키는 원격 URL만 바꾸면 되는데, 지금 기록해 둔 HTTPS 시절의 `git remote -v` 출력을 지우지 않고 두었으므로 나중에 SSH 전환 후의 출력을 아래에 덧붙이면 인증 방식 차이를 보여주는 전후 비교 자료가 됩니다.

README 3장의 체크리스트도 필수 항목과 보너스 항목을 물리적으로 나누어, 보너스를 추가할 때 수정할 지점이 체크박스 다섯 줄로만 국한되게 해두었습니다.
