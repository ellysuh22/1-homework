# 쉬운 설명서 — 내가 한 과제, 한 번에 이해하기

> README는 평가자용 문서라 딱딱합니다. 이 문서는 **내가 이해하고 남에게 설명하기 위한** 문서입니다.
> 중학생도 알아들을 수 있는 말로 다시 썼습니다. 순서대로 읽으면 전체가 하나로 연결됩니다.

---

# 0. 30초 요약

누가 물어보면 이렇게 답하면 됩니다.

> "내 컴퓨터에서만 되는 프로그램 말고, **누구 컴퓨터에서든 똑같이 돌아가는 프로그램**을 만드는 연습이었어요.
> 그러려면 세 가지가 필요해요. 컴퓨터한테 글자로 명령하는 법(**터미널**), 프로그램을 실행 환경째로 포장하는 법(**도커**), 그리고 만든 걸 인터넷에 올려 공유하는 법(**깃허브**)이요.
> 저는 웹사이트 하나를 도커로 포장해서 실제로 브라우저에 띄우고, 그 과정을 전부 증거로 남겼어요."

---

# 1. 왜 이걸 배우나 — 문제부터 이해하기

## 개발자들이 진짜로 겪는 싸움

```
A: "제 컴퓨터에서는 잘 되는데요?"     ← It works on my machine
B: "저는 실행하면 에러 나는데요?"
```

같은 코드인데 왜 다를까요? **두 사람의 컴퓨터 환경이 다르기 때문**입니다.

- A는 윈도우, B는 맥
- A는 파이썬 3.9, B는 파이썬 3.12
- A는 설정 파일이 있고, B는 없고

프로그램 하나가 돌아가려면 코드만 있으면 되는 게 아닙니다. 운영체제, 프로그램 버전, 설정 파일이 전부 맞아야 합니다.

## 그래서 나온 해결책

> **"코드만 주지 말고, 환경을 통째로 싸서 주자."**

이게 **도커(Docker)** 입니다. 이 과제의 핵심입니다.

## 세 가지 도구

| 도구 | 한마디로 | 비유 |
|---|---|---|
| **터미널** | 컴퓨터한테 글자로 명령하기 | 마우스 클릭 대신 "말"로 시키기 |
| **도커** | 실행 환경을 상자에 포장하기 | 이삿짐을 컨테이너 박스에 통째로 넣기 |
| **깃 / 깃허브** | 작업 기록 남기고 공유하기 | 문서 수정 이력 자동 저장 + 구글 드라이브 |

---

# 2. 터미널 — 컴퓨터에게 글자로 말하기

## 왜 마우스 안 쓰고 이걸 쓰나?

세 가지 이유가 있습니다.

1. **서버에는 화면이 없다.** 회사 서버는 모니터가 없습니다. 글자로만 조작해야 합니다.
2. **반복 작업에 유리하다.** 파일 1000개 이름 바꾸기는 클릭으로 못 합니다. 명령 한 줄이면 됩니다.
3. **기록이 남는다.** 뭘 했는지 그대로 복사해 남길 수 있습니다.

## 내가 실습한 명령 9가지

| 명령 | 뜻 | 하는 일 |
|---|---|---|
| `pwd` | print working directory | 나 지금 어디 있어? |
| `ls -la` | list | 여기 뭐 있어? (숨은 것까지) |
| `cd` | change directory | 저기로 가자 |
| `mkdir` | make directory | 폴더 만들어 |
| `touch` | — | 빈 파일 만들어 |
| `cp` | copy | 복사해 |
| `mv` | move | 옮기거나 이름 바꿔 |
| `rm` | remove | 지워 |
| `cat` | — | 파일 내용 보여줘 |

💡 **`mv`가 두 가지 일을 하는 이유**: 같은 폴더 안에서 옮기면 "이름 변경"이 되고, 다른 폴더로 옮기면 "이동"이 됩니다. 컴퓨터 입장에선 똑같은 동작입니다.

---

# 3. 경로 — 절대 경로 vs 상대 경로 ⭐

과제 목표 6가지 중 첫 번째입니다. **꼭 이해해야 합니다.**

## 비유로 이해하기

**절대 경로 = 도로명 주소**

> "서울시 강남구 테헤란로 123"

부산에서 출발하든 대전에서 출발하든 **똑같은 곳**을 가리킵니다.

```
/Users/ellysuh/Documents/1-homework/practice/hello.txt
└ 맨 앞이 / 로 시작 = 절대 경로
```

**상대 경로 = 길 안내**

> "여기서 두 블록 직진, 오른쪽"

**어디서 출발하느냐에 따라 도착지가 달라집니다.**

```
./hello.txt      →  . 은 "지금 있는 폴더"
../docs          →  .. 은 "한 단계 위 폴더"
```

## 내가 실제로 증명한 것

같은 명령인데 **위치만 바꿨더니 결과가 달라졌습니다.**

```bash
# practice 폴더에 있을 때
$ cat ./hello.txt
첫 번째 줄입니다.        ← 성공 ✅

# 한 칸 아래 docs 폴더로 이동한 뒤, 똑같은 명령
$ cd practice/docs
$ cat ./hello.txt
No such file or directory   ← 실패 ❌
```

📢 **남에게 설명할 때**: "상대 경로는 내가 서 있는 자리가 기준이라서, 자리를 옮기면 같은 명령도 다른 걸 가리켜요. 절대 경로는 어디서 하든 똑같고요."

---

# 4. 권한 — 755, 644가 뭔가요? ⭐

과제 목표 두 번째입니다. 숫자가 나와서 어려워 보이지만 **덧셈일 뿐**입니다.

## 1단계: 3종류의 권한

| 기호 | 뜻 | 파일일 때 | 폴더일 때 |
|---|---|---|---|
| `r` | read (읽기) | 내용 보기 | 목록 보기 |
| `w` | write (쓰기) | 내용 고치기 | 파일 만들기/지우기 |
| `x` | execute (실행) | 프로그램 실행 | **폴더 안에 들어가기** |

⚠️ **폴더의 `x`는 "실행"이 아니라 "들어가기"입니다.** 이게 함정입니다.

## 2단계: 3무리의 사람

```
소유자(나)  →  그룹(내 팀)  →  기타(나머지 전부)
```

## 3단계: `ls -l` 읽는 법

```
-rwxr-xr-x
│└┬┘└┬┘└┬┘
│ │  │  └── 기타:   r-x
│ │  └───── 그룹:   r-x
│ └──────── 소유자: rwx
└────────── 종류 (- 는 파일, d 는 폴더)
```

## 4단계: 숫자로 바꾸기 — **r=4, w=2, x=1**

있는 것만 더하면 됩니다.

| 권한 | 계산 | 숫자 |
|---|---|---|
| `rwx` | 4+2+1 | **7** |
| `rw-` | 4+2 | **6** |
| `r-x` | 4+1 | **5** |
| `r--` | 4 | **4** |
| `---` | 0 | **0** |

그래서:

```
755  =  7      5      5     =  rwx  r-x  r-x
        소유자  그룹   기타       ↑
                              폴더의 표준값
                              (남도 들어올 수는 있지만 수정은 못 함)

644  =  6      4      4     =  rw-  r--  r--
                                   ↑
                              문서 파일의 표준값
                              (나만 고칠 수 있고 남은 읽기만)
```

## 왜 폴더는 644가 아니라 755일까?

제가 직접 실험했습니다. 폴더를 644로 만들면 `x`가 사라집니다.

```bash
$ chmod 644 practice/perm-demo    # x 제거
$ ls practice/perm-demo
Permission denied                  ← 내 폴더인데 못 들어감! ❌
```

📢 **남에게 설명할 때**: "폴더는 x가 있어야 들어갈 수 있어요. 그래서 폴더는 755, 파일은 644를 써요."

---

# 5. 도커 — 이 과제의 핵심 ⭐⭐⭐

## 5-1. 이미지 vs 컨테이너 — 붕어빵으로 이해하기

**가장 중요한 개념입니다.**

```
   붕어빵 틀              →           붕어빵
   (이미지)                          (컨테이너)

   · 하나만 있으면 됨                 · 여러 개 찍어낼 수 있음
   · 모양이 안 변함(읽기 전용)        · 실제로 존재하고 먹을 수 있음
   · 그 자체로는 못 먹음              · 하나 먹어도 틀은 그대로
```

| | 이미지 | 컨테이너 |
|---|---|---|
| 정체 | 설계도 / 틀 | 실제로 돌아가는 것 |
| 개수 | 1개 | 여러 개 가능 |
| 만드는 법 | `docker build` | `docker run` |

**제가 실제로 증명한 것**: `my-web:1.0` 이미지 **하나**로 컨테이너 **세 개**(8080, 8081, 8082)를 동시에 띄웠습니다.

## 5-2. Dockerfile — 붕어빵 틀을 만드는 레시피

```
Dockerfile  →(docker build)→  이미지  →(docker run)→  컨테이너
  레시피                        틀                      붕어빵
```

제가 쓴 레시피를 쉬운 말로 옮기면:

| 명령어 | 쉬운 말 |
|---|---|
| `FROM nginx:1.29-alpine` | "웹서버 깔린 완제품에서 시작할게" |
| `LABEL ...` | "이 이미지 이름표 붙여" |
| `ENV APP_PORT=80` | "포트 번호는 밖에서 바꿀 수 있게 해둬" |
| `COPY app/ ...` | "내가 만든 웹페이지를 넣어" |
| `COPY templates/ ...` | "서버 설정도 내 걸로 바꿔" |
| `EXPOSE 80` | "80번 포트 쓸 거야 (메모)" |
| `HEALTHCHECK ...` | "10초마다 살아있나 확인해" |

💡 **왜 `nginx`부터 시작하나?** 김밥 만들 때 쌀농사부터 짓지 않는 것과 같습니다. 이미 밥이 된 걸 가져다 쓰는 게 당연합니다.

## 5-3. 포트 매핑 — 왜 필요한가? ⭐

과제 목표 네 번째입니다.

### 포트가 뭔데?

컴퓨터 한 대에서 여러 프로그램이 인터넷을 씁니다. 누구한테 온 신호인지 구분하려면 **번호표**가 필요합니다. 그게 포트입니다.

> 아파트로 치면 → **건물 주소는 하나(IP), 호수는 여러 개(포트)**

### 왜 다리를 놓아야 하나?

컨테이너는 **격리된 방**입니다. 방 안에서 아무리 소리쳐도 밖에서는 안 들립니다.

```
   내 맥(호스트)          │        컨테이너 (격리된 방)
                          │
   브라우저 →  8080번 ────┼────→  80번 → nginx 웹서버
                          │
                     이 다리가 -p 옵션
```

```bash
docker run -p 8080:80 my-web:1.0
              └─┬─┘ └┬┘
           내 맥의  컨테이너의
            8080     80번
```

### 제가 직접 증명한 것

다리를 **안 놓고** 실행해봤습니다.

```bash
$ docker run -d --name web-noport my-web:1.0      # -p 없음!

$ curl http://localhost:8082
Failed to connect                    ← 밖에서는 안 닿음 ❌

$ docker exec web-noport wget -O - http://127.0.0.1:80/env
APP_ENV=dev                          ← 근데 안에서는 잘 돌고 있음! ✅
```

📢 **남에게 설명할 때**: "서버는 멀쩡히 돌고 있는데 다리가 없어서 못 닿는 거예요. `-p`가 그 다리예요."

### ⚠️ 헷갈리는 것: `EXPOSE`는 다리가 아니다

`EXPOSE 80`은 그냥 **"저 80번 쓸 거예요"라고 적어둔 메모**입니다. 실제로 문을 여는 건 `-p`뿐입니다.

### 포트는 하나씩만 쓸 수 있다

같은 8080을 두 번 쓰려고 하면 에러가 납니다.

```bash
$ docker run -d -p 8080:80 --name web-conflict my-web:1.0
Bind for 0.0.0.0:8080 failed: port is already allocated   ❌
```

그래서 두 번째 컨테이너는 8081을 썼습니다. **컨테이너 안쪽 80번은 겹쳐도 됩니다** — 서로 격리돼 있으니까요.

## 5-4. 바인드 마운트 — 실시간으로 연결하기

### 문제 상황

웹페이지 글자 하나 고칠 때마다 이미지를 다시 만들어야 한다면? → 너무 느립니다.

### 해결

**내 컴퓨터 폴더를 컨테이너 안에 그대로 연결**해버립니다.

```
내 맥의 bindmount-demo/  ←──── 같은 파일! ────→  컨테이너의 /usr/share/nginx/html/
```

```bash
docker run -v /내폴더경로:/컨테이너경로 my-web:1.0
```

### 제가 증명한 것

```bash
# 처음
$ curl localhost:8082
v1                                    ← 화면에 v1

# 내 맥에서 파일만 수정 (재빌드 X, 재시작 X)
$ sed -i '' 's|>v1<|>v2<|' bindmount-demo/index.html

# 바로 다시 요청
$ curl localhost:8082
v2                                    ← 즉시 바뀜! ✨
```

📢 **남에게 설명할 때**: "폴더를 연결해두면 내 컴퓨터에서 고치는 순간 컨테이너 안에서도 바뀌어요. 개발할 때 진짜 편해요."

## 5-5. 볼륨 — 데이터 지키기 ⭐

과제 목표 다섯 번째입니다.

### 문제 상황

**컨테이너를 지우면 안에 있던 데이터도 같이 사라집니다.**

쇼핑몰이라면? 주문 기록이 전부 날아갑니다. 큰일입니다.

### 해결

데이터를 컨테이너 밖, **도커가 관리하는 창고**에 보관합니다. 컨테이너는 그 창고를 빌려 쓰기만 합니다.

```
   컨테이너 A  ─┐
                ├──→  볼륨 (창고)  ← 컨테이너를 지워도 창고는 남음
   컨테이너 B  ─┘
```

### 제가 증명한 방법 — 실험 2개를 나란히

똑같은 절차를 **볼륨 있이 / 없이** 두 번 해서 비교했습니다.

| | 실험 A (볼륨 O) | 실험 B (볼륨 X) |
|---|---|---|
| 1. 파일 저장 | `기록 시각: 11:42:47` | `데이터 저장함` |
| 2. 컨테이너 삭제 | 삭제 | 삭제 |
| 3. 새 컨테이너에서 확인 | **`기록 시각: 11:42:47`** ✅ | `No such file` ❌ |

두 실험의 **차이는 볼륨 하나뿐**입니다. 그러니 데이터를 지켜준 건 볼륨입니다.

📢 **남에게 설명할 때**: "볼륨 없이 하면 컨테이너 지울 때 데이터도 사라지는데, 볼륨 쓰면 시간 기록까지 그대로 남아 있었어요. 둘 다 해봐서 비교했어요."

### 바인드 마운트 vs 볼륨 — 뭐가 다른가?

| | 바인드 마운트 | 볼륨 |
|---|---|---|
| 어디에 저장? | **내가 정한** 내 컴퓨터 폴더 | **도커가 알아서** 관리하는 창고 |
| 주로 언제 씀? | 개발 중 (코드 고치며 확인) | 실제 운영 (DB 데이터 보관) |
| 비유 | 내 책상 서랍을 그대로 연결 | 창고에 맡기고 번호표 받기 |

## 5-6. attach vs exec — 들어가는 두 가지 방법

과제에서 "차이를 직접 관찰하라"고 한 부분입니다.

```
 exec  = 그 집에 문을 하나 더 뚫고 들어가기
 attach = 이미 열려 있는 문으로 들어가서 주인이랑 같은 화면 보기
```

### 실험 결과

```bash
# exec 로 들어갔다 나오기
$ docker exec tick-demo sh -c 'echo 안녕'
안녕
$ docker ps
tick-demo   Up 4 seconds            ← 컨테이너 멀쩡 ✅

# attach 로 들어가서 Ctrl+C
$ docker attach tick-demo
tick 6
tick 7
^C
$ docker ps -a
tick-demo   Exited (130)            ← 컨테이너 죽음 ❌
```

💡 **130이라는 숫자의 의미**: `128 + 2`입니다. 2번은 Ctrl+C 신호(SIGINT) 번호입니다. 즉 "Ctrl+C 때문에 죽었다"는 뜻입니다.

📢 **남에게 설명할 때**: "attach는 주인이랑 같은 화면을 보는 거라, 내가 Ctrl+C 누르면 주인도 같이 죽어요. exec은 따로 문 내고 들어간 거라 나가도 멀쩡하고요. 그래서 실행 중인 컨테이너 건드릴 땐 exec을 써야 해요."

---

# 6. 깃 vs 깃허브 ⭐

과제 목표 여섯 번째입니다. **가장 많이 헷갈리는 것**입니다.

## 한 줄 정리

```
깃(Git)      = 내 컴퓨터에 설치된 프로그램.  변경 이력을 기록.
깃허브(GitHub) = 인터넷 사이트.              그 기록을 올려서 공유.
```

## 비유

| | 깃 | 깃허브 |
|---|---|---|
| 비유 | 워드의 "변경 내용 추적" 기능 | 구글 드라이브 |
| 어디서 돌아감 | 내 컴퓨터 | 인터넷 |
| 인터넷 필요? | ❌ 없어도 됨 | ✅ 필요 |
| 하는 일 | 저장·기록·되돌리기 | 공유·협업·백업 |

## 왜 깃을 쓰나?

이런 파일 만들어본 적 있으시죠?

```
보고서.docx
보고서_최종.docx
보고서_최종_진짜최종.docx
보고서_최종_진짜최종_이거씀.docx      😱
```

깃은 파일 하나로 **모든 변경 이력을 자동 관리**합니다. 언제든 예전으로 돌아갈 수 있습니다.

## 제가 이 차이를 몸으로 느낀 사건

과제하다가 **깃허브에서 두 번 막혔습니다.**

1. 계정이 두 개인데 **저장소 주인이 아닌 계정**으로 로그인돼 있어서 업로드 거부
2. 계정 바꿨더니, 이번엔 **이메일 보호 설정** 때문에 또 거부

그런데 **내 컴퓨터의 깃 작업은 아무 문제가 없었습니다.** 저장도 잘 됐고 기록도 멀쩡했습니다. 막힌 건 전부 인터넷 쪽(깃허브)이었습니다.

📢 **남에게 설명할 때**: "깃은 내 컴퓨터에서 기록하는 거고, 깃허브는 그걸 인터넷에 올리는 거예요. 저는 컴퓨터 작업은 다 됐는데 인터넷 올릴 때만 막혀서, 둘이 완전 별개라는 걸 확실히 알았어요."

---

# 7. 내가 실제로 한 일

## 만든 것

```
웹사이트 하나 (HTML/CSS)
    ↓ Dockerfile 로 포장
my-web:1.0 이미지
    ↓ 실행
컨테이너 3개 (8080, 8081, 8082)
    ↓ 브라우저로 확인
✅ 작동 확인 + 스크린샷
```

## 남긴 증거

| 증거 | 개수 |
|---|---|
| 터미널 로그 파일 | 9개 |
| 스크린샷 | 4장 |
| 커밋 | 8개 |
| 총 파일 | 35개 이상 |

## 겪은 문제 5가지 (전부 실제 상황)

| 문제 | 원인 | 해결 |
|---|---|---|
| 깃허브 업로드 거부 | 다른 계정으로 로그인돼 있음 | 주인 계정으로 다시 로그인 |
| 로그가 중간에 끊김 | 상대 경로 써서 위치 바뀌니 깨짐 | 절대 경로로 바꿈 |
| 컨테이너 안 켜짐 | 8080 포트 이미 사용 중 | 8081로 변경 |
| 또 업로드 거부 | 깃허브가 내 이메일 보호 중 | 설정 변경 |
| 스크린샷이 안 찍힘 | 맥이 화면 녹화 권한 막음 | 권한 허용 |

💡 **두 번째 문제가 재밌습니다.** "절대 경로 vs 상대 경로"를 설명하는 실습을 하다가, 정작 **내가 상대 경로 때문에 로그를 날렸습니다.** 배우려던 걸 직접 당한 셈입니다.

---

# 8. 발표 대본 (5분 버전)

누가 "뭐 했어?" 물으면 이 순서로 말하면 됩니다.

### ① 문제 제기 (30초)
> "개발할 때 '내 컴퓨터에서는 되는데요?' 하는 문제가 진짜 많대요. 컴퓨터마다 환경이 달라서요. 그걸 해결하는 게 이번 과제였어요."

### ② 해결책 소개 (30초)
> "도커라는 걸 쓰면 프로그램을 **실행 환경째로 상자에 포장**할 수 있어요. 그 상자를 어디서 열든 똑같이 돌아가요."

### ③ 핵심 개념 (1분)
> "도커에는 **이미지**랑 **컨테이너**가 있는데, 붕어빵 틀이랑 붕어빵 같은 거예요. 틀 하나로 붕어빵 여러 개 찍듯이, 이미지 하나로 컨테이너 여러 개를 띄울 수 있어요. 저는 실제로 하나로 3개 띄워봤어요."

### ④ 내가 한 것 (1분 30초)
> "웹사이트 하나 만들어서 도커로 포장했어요. 그리고 세 가지를 증명했어요.
> 첫째, **포트 매핑**. 컨테이너는 격리돼 있어서 다리를 안 놓으면 밖에서 못 들어가요. 일부러 다리 없이 띄워서 접속 실패하는 것도 보여줬어요.
> 둘째, **바인드 마운트**. 내 폴더를 연결해두면 파일 고치는 순간 바로 반영돼요.
> 셋째, **볼륨**. 컨테이너 지워도 데이터가 살아남아요. 볼륨 없이도 해봐서 비교했어요."

### ⑤ 배운 점 (1분)
> "제일 크게 배운 건 **증명하는 방법**이에요. 그냥 '됐어요' 하는 것보다, 안 되는 경우랑 나란히 놓고 비교하면 왜 필요한지가 확실해지더라고요.
> 그리고 하다가 문제를 5번 겪었는데, 그중에 제가 설명하려던 개념(상대 경로) 때문에 제가 당한 것도 있어서 기억에 확실히 남았어요."

### ⑥ 마무리 (30초)
> "결과물은 깃허브에 다 올려놨고, README만 보면 누구든 똑같이 따라 할 수 있게 명령어랑 결과를 전부 기록해뒀어요."

---

# 9. 예상 질문 대비

**Q. 도커랑 가상머신(VM)이랑 뭐가 달라요?**
> VM은 아파트를 새로 짓는 거고, 도커는 한 건물 안에 칸막이를 치는 거예요. VM은 운영체제를 통째로 하나 더 돌려서 무겁고 느리고, 도커는 이미 있는 걸 나눠 쓰니까 가볍고 빨라요. VM은 켜는 데 몇 분, 도커는 몇 초예요.

**Q. 이미지랑 컨테이너 차이가 뭐예요?**
> 붕어빵 틀이랑 붕어빵이요. 틀(이미지)은 안 변하고, 붕어빵(컨테이너)은 여러 개 만들 수 있고 하나 먹어도 틀은 남아요.

**Q. 포트 매핑이 왜 필요해요?**
> 컨테이너가 격리된 방이라서요. 방 안에서 서버가 돌아도 밖에서는 안 닿아요. `-p`로 다리를 놔야 브라우저에서 들어갈 수 있어요.

**Q. 볼륨이랑 바인드 마운트 차이는요?**
> 둘 다 데이터를 컨테이너 밖에 두는 건데, 바인드 마운트는 **내가 정한 내 컴퓨터 폴더**를 쓰고 볼륨은 **도커가 알아서 관리하는 창고**를 써요. 개발할 땐 바인드 마운트, 진짜 데이터 보관은 볼륨을 써요.

**Q. 깃이랑 깃허브 차이는요?**
> 깃은 내 컴퓨터에 깔린 프로그램이라 인터넷 없어도 되고, 깃허브는 그 결과를 올리는 인터넷 사이트예요. 깃으로 기록하고 깃허브로 공유해요.

**Q. 755랑 644가 뭐예요?**
> r=4, w=2, x=1로 놓고 더한 거예요. 755는 rwx r-x r-x라서 나만 다 되고 남은 읽기랑 들어가기만 되는 거고, 644는 rw- r-- r--이라 나만 고칠 수 있고 남은 읽기만 돼요. 폴더는 x가 있어야 들어갈 수 있어서 755를 써요.

**Q. OrbStack이 뭐예요?**
> 맥에서 도커를 쓰게 해주는 프로그램이에요. 도커는 원래 리눅스 기술이라 맥에서 그냥은 안 돌아가거든요. 학교 컴퓨터는 관리자 권한이 막혀 있어서, 권한 없이도 되는 OrbStack을 썼어요. 명령어는 똑같아요.

---

# 10. 용어 카드 (외울 필요는 없고, 찾아보기용)

| 용어 | 한마디 |
|---|---|
| 터미널 | 글자로 컴퓨터 조작하는 창 |
| 절대 경로 | `/`부터 시작. 어디서든 같은 곳 |
| 상대 경로 | 지금 위치 기준. `.`은 여기, `..`은 위 |
| 권한 r/w/x | 읽기 / 쓰기 / 실행(폴더는 진입) |
| 이미지 | 붕어빵 틀. 읽기 전용 |
| 컨테이너 | 붕어빵. 실제 돌아가는 것 |
| Dockerfile | 이미지 만드는 레시피 |
| 포트 매핑 | 내 컴퓨터 ↔ 컨테이너 다리 (`-p`) |
| EXPOSE | 다리 아님. 그냥 메모 |
| 바인드 마운트 | 내 폴더를 컨테이너에 연결 |
| 볼륨 | 도커가 관리하는 데이터 창고 |
| 헬스체크 | 살아있나 주기적으로 확인 |
| 깃 | 내 컴퓨터의 기록 프로그램 |
| 깃허브 | 인터넷 공유 사이트 |
| 커밋 | 저장 지점 하나 |
| 푸시 | 인터넷에 올리기 |

---

> 📄 더 자세한 내용과 실제 명령어·출력은 [README.md](README.md) 에 있습니다.
> 왜 그렇게 만들었는지에 대한 설명은 [REPORT.md](REPORT.md) 에 있습니다.
