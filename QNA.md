# 평가 문항 대비 Q&A

> 과제 평가표 5개 항목 18개 문항에 대한 답변입니다.
> 각 답변은 **① 한 줄 답 → ② 자세한 설명 → ③ 증거 위치** 순서로 되어 있습니다.
> 면접에서는 ①만 말하고, 더 물으면 ②를 이어가면 됩니다.

---

# 항목 1 — 기능 동작 검증

> 이 항목은 "했느냐"를 묻는 항목입니다. 증거 위치만 정확히 짚으면 됩니다.

## Q1-1. 터미널에서 기본 명령어로 폴더/파일 생성·이동·삭제를 수행한 흔적이 있는가?

**① 한 줄 답**
> 네. 미션이 요구한 9가지 조작을 전부 수행했고, 명령어와 출력이 짝으로 기록된 로그가 있습니다.

**② 자세한 설명**
`pwd`(위치 확인), `ls -la`(숨김 파일 포함 목록), `cd`(이동), `mkdir`(폴더 생성), `touch`(빈 파일 생성), `cp`(복사), `mv`(이동/이름변경), `rm`(삭제), `cat`(내용 확인) 아홉 가지입니다.

특히 `mv`는 **같은 폴더 안에서 옮기면 이름 변경, 다른 폴더로 옮기면 이동**이 되므로 두 경우를 각각 실행해 구분했습니다.

**③ 증거**
[`logs/01-terminal.log`](logs/01-terminal.log) — 실행 스크립트는 [`scripts/01-terminal.sh`](scripts/01-terminal.sh)

---

## Q1-2. 파일 권한 변경 결과가 확인되는가?

**① 한 줄 답**
> 네. 파일과 디렉토리 각각에 대해 변경 전/후를 대조했고, 권한이 실제로 작동하는지까지 확인했습니다.

**② 자세한 설명**
파일은 `644 → 600`, 디렉토리는 `755 → 700`으로 바꾸고 `ls -l` 결과를 전후로 비교했습니다. 표기가 바뀌는 것만 보이면 충분하지 않다고 생각해서, `chmod 000`으로 만든 뒤 `cat`을 실행해 **소유자인데도 `Permission denied`가 나는 것**까지 기록했습니다.

디렉토리에서는 `x` 권한을 제거하고 `ls`를 시도해 접근이 막히는 것을 보였습니다. 디렉토리의 `x`가 '실행'이 아니라 '진입' 권한이라는 걸 실패 출력으로 증명한 것입니다.

**③ 증거**
[`logs/02-permissions.log`](logs/02-permissions.log)

---

## Q1-3. `docker --version`이 출력되고, Docker가 동작 가능한 상태인가?

**① 한 줄 답**
> 네. 버전은 29.4.0이고, `docker info`의 Server 섹션이 정상 출력되어 데몬이 살아있음을 확인했습니다.

**② 자세한 설명**
`docker --version`은 **클라이언트**가 설치됐는지만 알려줍니다. 실제로 컨테이너를 만들고 굴리는 것은 **데몬(엔진)** 이라, 데몬이 죽어 있으면 버전은 나와도 아무것도 못 합니다. 그래서 `docker info`를 함께 실행해 Server 섹션이 에러 없이 나오는 것을 확인했습니다. 데몬이 죽어 있으면 `Cannot connect to the Docker daemon`이 뜹니다.

저는 macOS라 OrbStack으로 Docker 엔진을 돌렸고, `docker context ls`로 활성 컨텍스트가 `orbstack`인 것도 기록했습니다.

**③ 증거**
[`logs/03-docker-check.log`](logs/03-docker-check.log)

---

## Q1-4. `docker run hello-world`가 정상 실행되는가?

**① 한 줄 답**
> 네. `Hello from Docker!` 메시지가 정상 출력됐습니다.

**② 자세한 설명**
이 이미지는 메시지를 출력하고 곧바로 종료되는 설치 확인용 이미지입니다. 출력 내용 자체가 Docker의 동작 흐름을 설명해 줍니다 — 클라이언트가 데몬에 요청 → 데몬이 이미지를 받아옴 → 컨테이너 생성 → 실행 결과를 다시 클라이언트로 전달.

종료된 뒤에도 `docker ps -a`에는 기록이 남는다는 것도 함께 확인했습니다.

**③ 증거**
[`logs/04-container-basics.log`](logs/04-container-basics.log)

---

## Q1-5. 이미지/컨테이너 목록 확인 및 정리 흔적이 있는가?

**① 한 줄 답**
> 네. `docker images`, `docker ps`, `docker ps -a`로 목록을 확인했고 `docker stop`·`docker rm`으로 정리했습니다.

**② 자세한 설명**
이미지는 `docker pull`로 따로 내려받은 로그를 남겼습니다. `docker run`을 하면 이미지가 자동으로 받아지지만, 그러면 '다운로드' 자체가 증거로 남지 않기 때문입니다.

컨테이너는 `docker ps`(실행 중)와 `docker ps -a`(종료된 것 포함)를 구분해서 확인했고, 운영 명령으로 `docker logs`(로그 확인), `docker stats --no-stream`(리소스 확인), `docker stop`(중지), `docker rm`(삭제)을 모두 수행했습니다.

💡 `docker stats`는 화면을 계속 갱신하는 실시간 모드라 로그 파일로 못 남깁니다. `--no-stream`을 붙이면 현재 시점 스냅샷 1회만 출력하고 끝납니다.

**③ 증거**
[`logs/03-docker-check.log`](logs/03-docker-check.log), [`logs/04-container-basics.log`](logs/04-container-basics.log)

---

## Q1-6. Dockerfile로 이미지 빌드가 가능한가?

**① 한 줄 답**
> 네. 직접 작성한 Dockerfile로 `my-web:1.0` 이미지를 빌드했고 컨테이너 실행까지 성공했습니다.

**② 자세한 설명**
`nginx:1.29-alpine`을 베이스로 커스텀 포인트 6가지를 얹었습니다 — 메타데이터(LABEL), 설정값 분리(ARG/ENV), 정적 콘텐츠 교체(COPY app/), 서버 설정 교체(COPY templates/), 포트 선언(EXPOSE), 헬스체크(HEALTHCHECK).

빌드 로그는 `--progress=plain` 옵션으로 남겼습니다. 기본 모드는 진행률 애니메이션이라 로그 파일에 제어문자가 잔뜩 남기 때문입니다.

빌드가 됐다는 것만이 아니라 **커스텀 포인트가 실제로 이미지에 박혔는지**를 `docker inspect`로 하나씩 확인했고, 헬스체크가 `starting → healthy`로 바뀌는 것까지 기록했습니다.

**③ 증거**
[`Dockerfile`](Dockerfile) (지시어별 상세 주석 포함), [`logs/05-image-build.log`](logs/05-image-build.log)

---

## Q1-7. 매핑된 포트로 접속이 가능한가?

**① 한 줄 답**
> 네. 8080과 8081 두 포트로 접속에 성공했고, 주소창이 보이는 브라우저 스크린샷도 첨부했습니다.

**② 자세한 설명**
같은 이미지 하나로 컨테이너 두 개를 서로 다른 호스트 포트에 띄웠습니다.


**③ 증거**
[`logs/06-port-mapping.log`](logs/06-port-mapping.log), [`evidence/browser-8080.png`](evidence/browser-8080.png), [`evidence/browser-8081.png`](evidence/browser-8081.png)

---

## Q1-8. Docker 볼륨 데이터가 컨테이너 삭제 후에도 유지되는가?

**① 한 줄 답**
> 네. 컨테이너를 완전히 삭제한 뒤 새 컨테이너에서 읽었을 때 기록 시각까지 그대로 나왔습니다.

**② 자세한 설명**
볼륨을 만들어 컨테이너에 연결하고 파일을 기록했습니다. 그다음 `docker rm -f`로 컨테이너를 완전히 지우고, 같은 볼륨을 새 컨테이너에 연결해 읽었더니 `기록 시각: 2026-07-31 11:42:47`이 그대로 나왔습니다.

여기서 한 가지 더 했습니다. **볼륨을 쓰지 않는 대조군**으로 똑같은 절차를 밟아 `No such file or directory`가 나오는 것을 나란히 기록했습니다. 두 실험의 차이가 볼륨 연결 여부 하나뿐이므로, 데이터를 지켜준 것이 볼륨이라는 결론이 논리적으로 성립합니다.

**③ 증거**
[`logs/08-volume.log`](logs/08-volume.log)

---

## Q1-9. Git 설정 및 GitHub 연동이 확인되는가?

**① 한 줄 답**
> 네. `git config --list` 결과를 기록했고, GitHub 업로드와 VSCode 연동 스크린샷까지 있습니다.

**② 자세한 설명**
`user.name`, `user.email`, `init.defaultBranch main`을 설정하고 `git config --list` 전체 출력을 남겼습니다. 작업 단위를 나눠 커밋했고(골격 → 스크립트 → 로그 → 증거 → 문서), `git push`로 업로드한 뒤 `gh repo view`와 GitHub API로 실제 파일 수까지 확인했습니다.

VSCode 쪽은 계정 메뉴에 `ellysuh22 (GitHub)`가 표시된 스크린샷으로 로그인을 증명했고, `git branch -vv`로 로컬 브랜치가 원격을 추적하는 것도 CLI로 교차 확인했습니다.

**③ 증거**
[`logs/09-git-github.log`](logs/09-git-github.log), [`evidence/vscode-github-account.png`](evidence/vscode-github-account.png)

---

# 항목 2 — 동작 구조 설계

> 이 항목부터는 "왜 그렇게 했는지"를 묻습니다. 판단 근거를 말해야 합니다.

## Q2-1. 프로젝트 디렉토리 구조를 어떤 기준으로 구성했는가?

**① 한 줄 답**
> **역할별로 나눴습니다.** 이미지에 들어갈 것, 실행할 때만 쓰는 것, 증거로 남길 것을 폴더 단위로 분리했습니다.

**② 자세한 설명**

세 갈래로 나눴습니다.

| 폴더 | 역할 | 이미지에 들어가나? |
|---|---|---|
| `app/` | 웹페이지 내용 (HTML/CSS) | ✅ 들어감 |
| `templates/` | 서버 설정 | ✅ 들어감 |
| `bindmount-demo/` | 바인드 마운트 실습용 | ❌ 실행 시점에 연결 |
| `logs/`, `evidence/` | 수행 증거 | ❌ 제외 |
| `scripts/` | 실습 수행 도구 | ❌ 제외 |

이렇게 나눈 이유가 세 가지 있습니다.

**첫째, 이미지에 들어갈 것과 아닌 것을 명확히 하기 위해서입니다.** 로그나 스크린샷이 이미지에 섞여 들어가면 용량만 늘고 의미가 없습니다. `.dockerignore`로 `logs/`, `evidence/`, `scripts/`, `*.md`를 제외했습니다.

**둘째, 미션 문구에 맞췄습니다.** 미션이 "웹 서버 소스코드(예: `app/` 또는 `src/`)"를 Dockerfile과 **별개 산출물**로 나열해서, 흔히 쓰는 `site/` 대신 `app/`으로 두었습니다.

**셋째, 콘텐츠와 설정을 분리했습니다.** `app/`은 "무엇을 보여줄지", `templates/`는 "서버를 어떻게 굴릴지"입니다. Dockerfile에서 두 개의 `COPY`가 각각 다른 목적지(`/usr/share/nginx/html/`, `/etc/nginx/templates/`)로 가는 것도 이 구분 때문입니다.

**Dockerfile을 루트에 둔 것도 의도적입니다.** 하위 폴더로 옮기면 `docker build`에 경로 옵션을 따로 줘야 하고, 나중에 Docker Compose를 붙일 때도 설정이 복잡해집니다.

---

## Q2-2. 포트/볼륨 설정을 어떤 방식으로 재현 가능하게 정리했는가?

**① 한 줄 답**
> **포트는 대역을 미리 나눠 예약했고, 설정값은 환경변수로 빼서 명령 한 줄로 바꿀 수 있게 했습니다.** 그리고 모든 명령을 명령어+출력 짝으로 로그에 남겼습니다.

**② 자세한 설명**

**포트 — 대역 예약**

| 포트 | 용도 |
|---|---|
| 8080 | 포트 매핑 1회차 |
| 8081 | 포트 매핑 2회차 |
| 8082 | 바인드 마운트 실습 |
| 8090, 3000 | 보너스용 예약 (미사용) |

용도별로 미리 갈라두면 나중에 Compose를 띄워도 충돌이 나지 않습니다. 실제로 8080을 중복 사용해봤을 때 `port is already allocated` 에러가 났기 때문에, 이 문제를 미리 막아둔 것입니다.

**볼륨/설정 — 환경변수로 분리**

포트 번호를 설정 파일에 `listen 80`으로 박아두면 바꿀 때마다 파일을 고치고 이미지를 다시 빌드해야 합니다. 그래서 `ARG/ENV APP_PORT`로 선언하고, nginx 설정을 완성본이 아니라 **템플릿**으로 넣었습니다.

컨테이너가 시작될 때 nginx 공식 이미지의 스크립트가 `${APP_PORT}`를 실제 값으로 치환해 설정 파일을 만들어 줍니다. 덕분에 이미지는 그대로 두고 `docker run -e APP_PORT=3000` 한 줄로 포트를 바꿀 수 있습니다.

**재현성 — 로그 수집 방식 자체를 도구화**

미션에 "명령어 입력과 출력 결과가 함께 포함되어야 한다"는 조항이 있는데, 손으로 복붙하면 명령어를 빠뜨리기 쉽습니다. 그래서 `scripts/runlog.sh`에 헬퍼 함수를 만들어 모든 실습을 그 함수로 실행했습니다. 구조적으로 누락이 불가능합니다.

여기서 한 가지 더 신경 쓴 게 있습니다. 처음엔 명령어를 그대로 기록했더니 공백이 든 인자의 따옴표가 사라져서, 로그를 보고 따라 치면 다른 명령이 되는 문제가 있었습니다. 재현성이 깨지는 문제라 따옴표를 복원해 기록하도록 고쳤습니다. **지금 로그는 복사해 붙이면 그대로 실행됩니다.**

**③ 증거**
[`scripts/runlog.sh`](scripts/runlog.sh), [`README.md` 17장 재현 가이드](README.md)
→ 실제 사용법은 [부록 1 — 로그 남기는 법](#부록-1--로그-남기는-법-runlogsh-사용법)

---

# 항목 3 — 핵심 기술 원리 적용

## Q3-1. 이미지와 컨테이너의 차이를 "빌드/실행/변경" 관점에서 구분해 설명할 수 있는가?

**① 한 줄 답**
> **이미지는 붕어빵 틀, 컨테이너는 붕어빵입니다.** 틀은 빌드로 한 번 만들고 안 바뀌며, 붕어빵은 실행할 때마다 새로 생기고 여러 개 만들 수 있습니다.

**② 자세한 설명**

세 관점으로 나눠 보겠습니다.

| 관점 | 이미지 | 컨테이너 |
|---|---|---|
| **빌드** | `docker build`로 만든다. Dockerfile이 레시피 | 빌드로 만들어지지 않는다 |
| **실행** | 이미지 자체는 실행되지 않는다 | `docker run`으로 이미지에서 찍어낸다 |
| **변경** | **읽기 전용.** 고치려면 다시 빌드해야 한다 | 쓰기 가능. 단 지우면 변경분도 사라진다 |

**빌드 관점** — Dockerfile의 지시어 하나가 이미지의 '층(layer)' 하나를 만듭니다. 층은 캐시되기 때문에, 자주 바뀌는 내용을 뒤쪽에 둘수록 재빌드가 빨라집니다.

**실행 관점** — 이게 제일 중요합니다. **이미지 하나로 컨테이너를 여러 개 만들 수 있습니다.** 저는 `my-web:1.0` 하나로 8080·8081·8082 세 개를 동시에 띄웠습니다. 하나를 지워도 이미지와 다른 컨테이너는 멀쩡합니다.

**변경 관점** — 이미지는 읽기 전용이라 컨테이너에서 파일을 고쳐도 이미지는 안 바뀝니다. 그래서 컨테이너를 지우면 그 변경분이 함께 사라집니다. 이것이 **볼륨이 필요한 이유**로 이어집니다.

> 💡 실제로 이걸 보여준 사례가 있습니다. 바인드 마운트 실습에서 컨테이너를 완전히 지웠다가 다시 띄웠는데도 화면에 `v2`가 나왔습니다. 그 내용이 이미지가 아니라 **호스트 폴더**에 있었기 때문입니다. 이미지에는 `v1`도 `v2`도 들어있지 않습니다.

---

## Q3-2. 컨테이너 내부 포트로 직접 접속할 수 없는 이유와, 포트 매핑이 필요한 이유를 설명할 수 있는가?

**① 한 줄 답**
> **컨테이너가 격리된 공간이기 때문입니다.** 컨테이너는 자기만의 네트워크를 갖고 있어서, `-p`로 다리를 놓지 않으면 호스트에서 닿을 수 없습니다.

**② 자세한 설명**

컨테이너는 격리를 위해 **자기만의 네트워크 공간**을 가집니다. 컨테이너 안의 `localhost`는 호스트의 `localhost`와 완전히 다른 것입니다. 그래서 컨테이너 안에서 80번 포트가 열려 있어도 호스트에서는 보이지 않습니다.

`-p 8080:80`은 "호스트 8080번으로 오는 요청을 컨테이너 80번으로 넘겨줘"라는 다리를 놓는 것입니다.

**제가 직접 증명했습니다.** `-p` 없이 컨테이너를 띄웠더니:

```bash
$ curl http://localhost:8082
Failed to connect                    ← 호스트에서는 못 닿음

$ docker exec web-noport wget -O - http://127.0.0.1:80/env
APP_ENV=dev                          ← 그런데 컨테이너 안에서는 정상 응답
```

**서버는 멀쩡히 돌고 있는데 다리가 없어서 못 닿는 상황**을 그대로 재현한 것입니다.

**⚠️ 여기서 자주 오해하는 것: `EXPOSE`는 다리가 아닙니다.**

`EXPOSE 80`은 이미지에 "이 포트를 씁니다"라고 적어둔 **문서**일 뿐입니다. 실제로 여는 건 `-p`뿐입니다. 위 실험에서 `docker ps`의 PORTS 칸에 `80/tcp`만 있고 `0.0.0.0:8082->` 같은 호스트 포트가 없었던 것이 그 증거입니다.

**추가로 알아둘 것:** 충돌하는 건 **호스트 포트**이지 컨테이너 포트가 아닙니다. 8080·8081·8082 세 컨테이너가 안쪽으로는 모두 80번을 쓰지만 서로 격리돼 있어 아무 문제가 없습니다.

---

## Q3-3. 절대 경로와 상대 경로를 어떤 상황에서 선택하는가?

**① 한 줄 답**
> **위치가 바뀔 수 있으면 절대 경로, 같은 폴더 안에서 짧게 쓸 땐 상대 경로**를 씁니다. 스크립트나 설정 파일에는 절대 경로가 안전합니다.

**② 자세한 설명**

| | 절대 경로 | 상대 경로 |
|---|---|---|
| 형태 | `/`부터 전부 | `.` `..` 로 시작 |
| 기준 | 항상 루트 | **지금 있는 위치** |
| 비유 | 도로명 주소 | "여기서 두 블록 직진" |

**선택 기준은 "실행 위치가 바뀔 수 있는가"입니다.**

- **절대 경로를 써야 할 때**: 스크립트 안, 설정 파일, 도커 볼륨 마운트 경로, cron 같은 자동 실행. 어디서 실행될지 모르기 때문입니다.
- **상대 경로가 편한 때**: 터미널에서 손으로 칠 때, 프로젝트 안에서 이웃 파일을 참조할 때(README에서 `logs/01-terminal.log`처럼). 짧고 프로젝트를 통째로 옮겨도 유지됩니다.

**제가 실제로 겪은 사고가 있습니다.**

로그 파일 경로를 `logs/01-terminal.log`라는 상대 경로로 지정했는데, 스크립트가 `cd`로 하위 폴더에 들어간 순간부터 로그가 통째로 끊겼습니다.

```
scripts/runlog.sh: line 38: logs/01-terminal.log: No such file or directory
```

> 💡 참고로 바인드 마운트에서 호스트 경로에 반드시 절대 경로를 써야 하는 것도 같은 이유입니다. 그래서 `$(pwd)`를 사용해 현재 위치를 절대 경로로 바꿔 넣었습니다.

---

## Q3-4. 파일 권한 숫자 표기(755, 644)가 어떤 규칙으로 결정되는가?

**① 한 줄 답**
> **r=4, w=2, x=1로 놓고 가진 것만 더한 값**을 소유자·그룹·기타 순서로 나열한 것입니다.

**② 자세한 설명**

**1단계 — 권한 3종류**

| 기호 | 파일일 때 | 폴더일 때 |
|---|---|---|
| `r` (4) | 내용 읽기 | 목록 보기 |
| `w` (2) | 내용 수정 | 파일 생성/삭제 |
| `x` (1) | 프로그램 실행 | **폴더 안에 들어가기** |

**2단계 — 대상 3그룹**: 소유자(user) / 그룹(group) / 기타(other)

**3단계 — 덧셈**

| 조합 | 계산 | 숫자 |
|---|---|---|
| `rwx` | 4+2+1 | 7 |
| `rw-` | 4+2 | 6 |
| `r-x` | 4+1 | 5 |
| `r--` | 4 | 4 |

**4단계 — 세 자리로 나열**

```
755 = rwx r-x r-x        644 = rw- r-- r--
      ↑   ↑   ↑                ↑   ↑   ↑
    소유자 그룹 기타          소유자 그룹 기타
```

**왜 폴더는 755, 파일은 644인가?**

핵심은 **폴더의 `x`가 '실행'이 아니라 '진입'** 이라는 점입니다. 폴더에 `x`가 없으면 그 안에 들어갈 수 없습니다. 그래서 폴더는 `x`가 필요해 755, 일반 문서는 실행할 일이 없으니 `x`를 빼고 644를 씁니다.

**제가 실험으로 확인했습니다.**

```bash
$ chmod 644 practice/perm-demo      # 폴더에서 x 제거
$ ls practice/perm-demo
Permission denied                    ← 내 폴더인데 못 들어감
```

또 `chmod 000`으로 만들면 **소유자여도** 읽을 수 없다는 것도 확인했습니다. 소유자라고 무조건 통과하는 게 아니라 권한 비트를 그대로 따릅니다. (root만 예외입니다.)

---

# 항목 4 — 심층 인터뷰

> 이 항목이 가장 중요합니다. **실제로 겪은 일**을 말해야 설득력이 있습니다.

## Q4-1. "호스트 포트가 이미 사용 중"이라 포트 매핑이 실패한다면, 어떤 순서로 원인을 진단하겠는가?

**① 한 줄 답**
> **에러 메시지 확인 → 누가 그 포트를 쓰는지 조회 → 도커 안/밖 구분 → 포트 변경 또는 기존 컨테이너 정리** 순서로 진단합니다. 실제로 겪어봤습니다.

** 포트매핑 실패는 주로 다른 프로세스가 포트 점유한 상황이라 프로세스 종료시킨 후에 포트를 가져오는 방법과 다른 빈 포트를 찾는 두가지 존재.

**💡 핵심 포인트 하나 더**

충돌하는 건 **호스트 포트**이지 컨테이너 포트가 아닙니다. `-p 8081:80`처럼 왼쪽만 바꾸면 됩니다. 오른쪽 80은 세 컨테이너가 다 같이 써도 격리돼 있어 문제없습니다. 이걸 모르면 컨테이너 포트까지 바꾸려다 설정이 꼬입니다.

**③ 증거**
[`logs/06-port-mapping.log`](logs/06-port-mapping.log)

---

## Q4-2. 컨테이너 삭제 후 데이터가 사라진 경험이 있다면, 이를 방지하기 위한 대안을 설명할 수 있는가?

**① 한 줄 답**
> 네. **일부러 재현해봤습니다.** 대안은 볼륨이고, 실제로 볼륨을 쓴 경우와 안 쓴 경우를 나란히 실험해 비교했습니다.

**② 자세한 설명**

**왜 사라지는가**

컨테이너에 쓴 파일은 컨테이너 전용 저장 층에 저장됩니다. 컨테이너를 지우면 그 층도 함께 삭제되므로 데이터가 같이 사라집니다. 이미지는 읽기 전용이라 변경분을 흡수하지 않습니다.



**대안 — 볼륨 (운영 데이터에 적합)**

데이터를 도커가 관리하는 별도 저장소에 두고 컨테이너가 빌려 쓰게 합니다.


**③ 증거**
[`logs/08-volume.log`](logs/08-volume.log)

---

## Q4-3. 이 미션에서 가장 어려웠던 지점과 해결 과정(가설 → 확인 → 조치)을 근거와 함께 설명할 수 있는가?

**① 한 줄 답**
> **GitHub 업로드가 두 번 연속 다른 이유로 막힌 것**이 가장 어려웠습니다. 로컬 작업은 멀쩡한데 원격에서만 막혀서, Git과 GitHub이 별개라는 걸 몸으로 배웠습니다.

**② 자세한 설명 — 가설 → 확인 → 조치**

### 1차 — 계정 권한 문제

**문제**
로컬 원격 주소는 `ellysuh22/1-homework`인데, GitHub CLI는 `ellysuh22-22`라는 다른 계정으로 로그인돼 있었습니다.

**가설**
계정이 두 개인데 인증된 계정이 저장소 주인이 아니라서 쓰기 권한이 없을 것이다.

**확인**
push를 시도해 실패를 보기 전에, API로 현재 계정의 권한을 직접 조회했습니다.

```bash
$ gh api repos/ellysuh22/1-homework --jq '.permissions'
{"admin":false,"maintain":false,"pull":true,"push":false,"triage":false}
```

`push: false` — 가설이 사실로 확인됐습니다.

**조치**
저장소 주인 계정으로 재인증했습니다. 다른 방법(새 저장소를 만들어 remote 변경)도 있었지만, 이미 만들어둔 제출용 저장소를 그대로 쓰기 위해 재인증을 택했습니다.

### 2차 — 이메일 프라이버시 정책

**문제**
권한을 해결하고 push했더니 **또** 거부됐습니다.

```
remote: error: GH007: Your push would publish a private email address.
! [remote rejected] main -> main (push declined due to email privacy restrictions)
```

**가설**
이번엔 권한이 아니라 정책 문제로 보였습니다. 커밋 작성자 이메일이 실제 주소인데, 계정에 "이메일을 노출하는 커맨드라인 push 차단" 옵션이 켜져 있어 GitHub이 대신 막아준 것으로 추정했습니다.

**확인**
```bash
$ git log -1 --format='%an <%ae>'
ellysuh22 <youngsuh0630@gmail.com>        ← 실제 이메일이 들어있음
```
에러가 안내한 설정 페이지에서 차단 옵션이 켜져 있는 것도 확인했습니다.

**조치**
선택지가 둘이었습니다.
1. GitHub 제공 noreply 이메일(`309754256+ellysuh22@users.noreply.github.com`)로 바꾸고 히스토리 재작성
2. 차단 옵션 해제

2번을 택해 해결했습니다.

**③ 이 경험에서 배운 것**

**첫째, Git과 GitHub은 완전히 별개입니다.** 두 번 다 로컬 Git에는 아무 문제가 없었습니다. 커밋도 정상이고 기록도 멀쩡했습니다. 막힌 건 전부 원격 쪽 — 권한과 정책이었습니다. 이걸 겪고 나니 "Git은 로컬 버전관리, GitHub은 원격 협업 플랫폼"이라는 설명이 확실히 와닿았습니다.

**둘째, 커밋 이메일은 시작 전에 정해야 합니다.** 커밋을 만든 뒤에 바꾸려면 히스토리 재작성이 필요합니다. 공개 저장소에 실제 이메일을 남기고 싶지 않다면 처음부터 noreply 주소로 설정하는 게 맞습니다.

**셋째, 에러를 만나기 전에 확인하는 습관이 생겼습니다.** 1차 때 무작정 push해서 실패를 봤다면 원인 파악에 시간이 더 걸렸을 겁니다. `gh api`로 권한을 먼저 조회하니 한 번에 확인됐습니다.

---

### 💬 다른 후보 답변 (이걸 물으면 이렇게)

**"기술적으로 가장 까다로웠던 건?"**
> `docker run -it`으로 컨테이너에 들어간 세션을 로그로 남기는 것이었습니다. `-it`은 가상 터미널을 요구해서 일반적인 방법으로는 파일에 안 담깁니다. macOS 내장 `script` 명령으로 가상 터미널을 할당하고, 컨테이너 안에서 색상 출력을 끄도록(`TERM=dumb`) 설정해서 `bash-5.2#` 프롬프트가 그대로 남은 깨끗한 로그를 얻었습니다.

**"어이없었던 실수는?"**
> 로그 파일 경로를 상대 경로로 써서 `cd` 이후 로그가 통째로 날아간 일입니다. 그때 하고 있던 실습이 하필 "절대 경로 vs 상대 경로 비교"였습니다. 설명하려던 개념을 제가 직접 당해서 확실히 기억에 남았습니다.

---

# 항목 5 — 보너스 평가 포인트

## Q5-1. 보너스 과제를 수행했는가?

**① 한 줄 답**
> 이번 제출에는 포함하지 않았습니다. 다만 **나중에 추가할 때 기존 산출물을 하나도 고치지 않아도 되도록 미리 설계해 두었습니다.**

**② 자세한 설명**

보너스 5개 항목이 기존 결과물에 어떤 영향을 주는지 먼저 따져봤습니다.

| 보너스 항목 | 추가되는 것 | 기존 파일 수정 |
|---|---|---|
| Compose 기초 | `docker-compose.yml` 신규 | 없음 |
| Compose 멀티 컨테이너 | 위 파일에 서비스 추가 | 없음 |
| Compose 운영 명령 | 로그만 추가 | 없음 |
| **환경 변수로 포트 변경** | 없음 | **없음 — 단, 지금 설계해둬야 함** |
| GitHub SSH 키 | `git remote set-url` | 없음 |

**결합점이 딱 하나 있었습니다 — 환경 변수 항목입니다.**

만약 nginx 설정에 `listen 80`을 그대로 뒀다면, 나중에 이 항목을 하려고 Dockerfile을 고치고, README의 Dockerfile 전문을 고치고, 빌드 로그까지 다시 찍어야 했을 것입니다.

그래서 기본 과제 단계에서 미리 `ARG/ENV APP_PORT` + nginx 템플릿 구조로 만들었습니다. 이제 보너스는 이 명령 한 줄이면 끝나고 **파일 수정이 0**입니다.

```bash
docker run -d -p 3000:3000 -e APP_PORT=3000 -e APP_ENV=prod my-web:1.0
```

이 구조가 낭비가 아닌 이유는, 미션 (A) 방식이 요구하는 "정적 콘텐츠**/설정**만 교체"에서 '설정 교체'에 정확히 해당하기 때문입니다. 기본 과제 요구도 동시에 만족합니다.

**추가로 준비해둔 것**

- **포트 대역 예약**: 기본 과제는 8080·8081·8082만 쓰고 8090·3000을 비워뒀습니다. 나중에 Compose를 띄워도 충돌하지 않습니다.
- **README 구조**: 체크리스트를 필수/보너스로 물리적으로 분리해서, 보너스를 추가할 때 수정할 지점이 체크박스 다섯 줄로만 국한됩니다.
- **SSH 전환 대비**: 지금 기록해둔 HTTPS 시절의 `git remote -v` 출력을 지우지 않았습니다. 나중에 SSH로 바꾼 뒤의 출력을 아래에 덧붙이면 **인증 방식 차이의 전후 비교 자료**가 됩니다.

---

# 부록 1 — 로그 남기는 법 (runlog.sh 사용법)

> 미션 제약: **"캡처/로그에는 명령어 입력과 출력 결과가 함께 포함되어야 한다"**
> 손으로 복붙하면 명령어를 빠뜨리기 쉬워서, 아예 도구로 만들었습니다.
> 이 도구로 실행하면 **명령어 누락이 구조적으로 불가능**합니다.

## 준비 — 이 세 줄

터미널을 열고 한 줄씩 칩니다. (맥 기본 셸 zsh에서 그대로 됩니다)

```bash
cd /Users/ellysuh/Documents/1-homework
```
```bash
export LOGFILE=$PWD/logs/demo.log
```
```bash
source scripts/runlog.sh
```

**아무 반응 없이 프롬프트가 다시 뜨면 성공입니다.** (성공하면 조용한 게 정상)

바로 확인:

```bash
logrun pwd
```

`$ pwd` 와 경로가 **짝으로** 나오면 준비 끝입니다.

> ⚠️ `logs/demo.log`로 지정한 이유 — 제출용 로그 9개(`01-terminal.log` ~ `09-git-github.log`)는 이미 완성돼 있습니다. 연습하다 덮어쓰지 않도록 연습은 `demo.log`에서 하세요.

## 명령어 4개

| 명령 | 언제 쓰나 | 예시 |
|---|---|---|
| `logrun` | 보통 명령 전부 | `logrun pwd` / `logrun docker ps -a` |
| `logsh` | `\|` `>` `&&` 등 셸 기호가 들어갈 때 (따옴표 필수) | `logsh 'docker images \| head -5'` |
| `logcd` | 디렉토리 이동 | `logcd app` |
| `lognote` | 로그에 소제목 달기 | `lognote 3단계 - 이미지 빌드` |

## 안 될 때 — 에러 메시지로 범인 찾기

**① `no such file or directory: scripts/runlog.sh`**
→ 프로젝트 폴더 밖에 서 있는 것. `cd /Users/ellysuh/Documents/1-homework` 부터 다시.

**② `LOGFILE: LOGFILE 환경변수로 로그 파일 경로를 지정하세요`**
→ `export LOGFILE=...` 을 건너뛰었거나, 터미널 창을 새로 열어서 설정이 날아간 것. `export` 줄부터 다시.

**③ `command not found: logrun`**
→ 가장 흔한 실수. `bash scripts/runlog.sh` 나 `./scripts/runlog.sh` 로 실행한 경우입니다. 반드시 **`source`** 여야 합니다.

> 💡 **왜 `source`여야 하나 (비유)**
> `bash 파일` 은 옆방에 사람을 보내 도구를 꺼내게 하는 것 — 방문을 닫으면 도구도 같이 사라집니다.
> `source 파일` 은 도구를 **내 책상 위에** 꺼내놓는 것.
> `logrun`은 내 책상에 있어야 쓸 수 있습니다.

## 알아둘 동작 3가지

**1. `cd`는 반드시 `logcd`로.**
`logrun cd app`은 효과가 없습니다. `logrun`은 명령을 별도의 방(서브셸)에서 실행해서, 그 방만 이동하고 내 위치는 그대로이기 때문입니다. 이동 후 `logrun pwd`로 확인하면 로그에 증거가 남습니다.

**2. 실패해도 멈추지 않습니다.**
에러를 일부러 재현하는 실습을 위해 계속 진행되며, 대신 로그에 `[exit code: 127]` 처럼 종료 코드가 기록됩니다.

**3. 이어붙이기(append)입니다.**
기존 내용을 덮어쓰지 않고 뒤에 쌓입니다. 파일 맨 위에는 수집 시각과 수집 방법이 헤더로 자동 기록됩니다 — "언제, 어떻게 뽑은 로그인가"에 대한 재현성 근거입니다.

## 실제 출력 예시

```
===============================================================
 demo.log
 수집 시각: 2026-08-01 21:50:57 KST
 수집 방법: scripts/runlog.sh (명령어+출력 동시 기록)
===============================================================

### runlog.sh 사용법 데모

$ pwd
/Users/ellysuh/Documents/1-homework

$ ls logs | wc -l
       9

$ cd app

$ pwd
/Users/ellysuh/Documents/1-homework/app

$ cat-없는명령
bash: cat-없는명령: command not found
[exit code: 127]
```

화면에 보이는 내용과 로그 파일 내용이 **헤더를 빼면 완전히 동일**합니다.

---

# 부록 2 — 면접 직전 5분 복습

| 질문 유형 | 핵심 키워드 |
|---|---|
| 이미지 vs 컨테이너 | 붕어빵 틀 : 붕어빵 / 하나로 여러 개 / 이미지는 읽기 전용 |
| 포트 매핑 왜 필요? | 컨테이너는 격리 / `-p`가 다리 / EXPOSE는 문서일 뿐 |
| 볼륨 왜 필요? | 컨테이너 지우면 데이터도 사라짐 / 대조 실험으로 증명 |
| 절대 vs 상대 경로 | 위치 바뀌면 상대 경로는 깨짐 / 스크립트엔 절대 경로 |
| 755 / 644 | r=4 w=2 x=1 덧셈 / 폴더의 x는 '진입' |
| Git vs GitHub | 로컬 프로그램 : 인터넷 사이트 / 실제로 원격에서만 막혔음 |
| 포트 충돌 진단 | 메시지 확인 → `docker ps` → `lsof -i` → 호스트 포트만 변경 |
| 가장 어려웠던 점 | GitHub 2연속 거부 (권한 → 이메일 정책) |

---

> 📄 명령어와 출력 원본은 [README.md](README.md)
> 설계 판단 근거는 [REPORT.md](REPORT.md)
> 쉬운 개념 설명은 [EXPLAIN.md](EXPLAIN.md)
