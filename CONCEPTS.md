# 개념 사전 — mission.txt 완전 해설

> 비전공자를 위한 상세 설명입니다. `mission.txt`에 등장하는 모든 용어를
> "왜 필요한가 → 무슨 뜻인가 → 실제로 어떻게 쓰는가" 순서로 풀어서 정리했습니다.
> 어려운 표현은 최대한 일상적인 비유로 바꿔서 설명했습니다.

---

## 목차

1. [왜 이런 걸 배우는가 (큰 그림)](#1-왜-이런-걸-배우는가-큰-그림)
2. [터미널 / 리눅스 CLI 개념](#2-터미널--리눅스-cli-개념)
3. [파일 경로 개념](#3-파일-경로-개념)
4. [파일 권한 개념](#4-파일-권한-개념)
5. [터미널 기본 명령어 사전](#5-터미널-기본-명령어-사전)
6. [Docker 핵심 개념](#6-docker-핵심-개념)
7. [Docker 명령어 사전](#7-docker-명령어-사전)
8. [Dockerfile 개념](#8-dockerfile-개념)
9. [포트 매핑](#9-포트-매핑)
10. [바인드 마운트 vs 볼륨(데이터 영속성)](#10-바인드-마운트-vs-볼륨데이터-영속성)
11. [Docker Compose (보너스 관련)](#11-docker-compose-보너스-관련)
12. [Git 개념](#12-git-개념)
13. [GitHub 개념 및 Git과의 차이](#13-github-개념-및-git과의-차이)
14. [SSH vs HTTPS 인증 (보너스 관련)](#14-ssh-vs-https-인증-보너스-관련)
15. [기술 문서 / README 관련 개념](#15-기술-문서--readme-관련-개념)
16. [보안·개인정보 관련 개념](#16-보안개인정보-관련-개념)
17. [트러블슈팅 기록법](#17-트러블슈팅-기록법)
18. [용어 요약표 (초압축 버전)](#18-용어-요약표-초압축-버전)

---

## 1. 왜 이런 걸 배우는가 (큰 그림)

미션 첫 문장이 핵심입니다:

> "개발은 코드를 작성하는 순간이 아니라, 환경을 세팅하는 순간부터 시작됩니다."

실제 개발팀에서 흔히 겪는 문제 하나를 예로 들면:

- A 개발자: "제 컴퓨터에서는 잘 돼요!" (It works on my machine)
- B 개발자: "저는 실행하니까 에러 나는데요?"

이 문제가 생기는 이유는 두 사람의 **컴퓨터 환경(OS, 설치된 프로그램 버전, 설정)** 이 다르기 때문입니다.
이를 해결하기 위한 3가지 도구가 이번 미션의 핵심입니다.

| 도구 | 역할 | 비유 |
|---|---|---|
| 터미널(CLI) | 컴퓨터에게 글자로 명령을 내리는 창구 | 마우스 클릭 대신 "말"로 지시하는 것 |
| Docker | "실행 환경 자체"를 하나의 상자에 포장해서 어디서든 똑같이 실행 | 이사할 때 짐을 통째로 컨테이너 박스에 넣어 옮기는 것 |
| Git/GitHub | 코드가 바뀐 역사를 기록하고, 여러 사람이 함께 작업할 수 있게 하는 것 | 문서의 "모든 수정 이력"을 자동 저장 + 온라인 공유 |

이 세 가지를 합치면: **"내 컴퓨터에서만 되는 코드"가 아니라 "누구의 컴퓨터에서도 똑같이 되는 코드"** 를 만들 수 있습니다.
이것이 미션에서 계속 강조하는 **"재현 가능한 환경(reproducibility)"** 이라는 개념입니다.

---

## 2. 터미널 / 리눅스 CLI 개념

### 터미널(Terminal)이란?
- 마우스로 아이콘을 클릭하는 대신, **글자로 된 명령어를 입력**해서 컴퓨터를 조작하는 프로그램/창입니다.
- 이런 방식을 **CLI (Command Line Interface, 명령줄 인터페이스)** 라고 하고,
  아이콘을 클릭하는 방식은 **GUI (Graphic User Interface)** 라고 합니다.
- CLI가 필요한 이유: 서버(원격 컴퓨터)는 대부분 화면(모니터)이 없습니다. 화면이 없어도 명령어만으로 모든 걸 조작할 수 있어야 하기 때문에, 개발자는 CLI에 익숙해져야 합니다. 또한 같은 작업을 반복하거나 자동화(스크립트)하기에도 CLI가 압도적으로 유리합니다.

### 쉘(Shell)이란?
- 터미널 안에서 "내가 입력한 명령어를 해석해서 실제로 컴퓨터에게 전달해주는 프로그램"이 쉘입니다.
- 터미널 = 껍데기(창), 쉘 = 그 안에서 명령을 처리하는 엔진, 이라고 이해하면 됩니다.
- 종류: `bash`, `zsh` (macOS 최신 버전 기본 쉘) 등이 있습니다.
- 확인 명령: `echo $SHELL`

### 프롬프트(Prompt)
- 터미널에서 명령어를 입력할 수 있게 기다리고 있는 상태의 표시(보통 `$` 또는 `%` 기호)입니다.
- 예: `user@macbook ~ %` 이런 식으로 "지금 명령을 받을 준비가 됐다"는 표시입니다.

### 명령어의 기본 구조
```
명령어 [옵션] [인자]
예: ls -la ~/Documents
     └명령  └옵션  └대상(인자)
```
- **옵션(option/flag)**: `-` 또는 `--`로 시작하며 명령의 동작 방식을 바꿉니다. (예: `-l`은 자세히, `-a`는 숨김파일 포함)
- **인자(argument)**: 명령이 작업할 대상(파일명, 경로 등)입니다.

---

## 3. 파일 경로 개념

### 루트(Root, `/`)와 홈 디렉토리(`~`)
- 리눅스/macOS의 모든 파일과 폴더는 `/` (루트) 라는 최상위 지점에서 시작하는 나무 구조(트리 구조)로 되어 있습니다.
- `~` 는 "현재 로그인한 사용자의 홈 폴더"를 뜻하는 축약 기호입니다. (예: `/Users/ellysuh` 를 `~`로 표현 가능)

### 절대 경로 (Absolute Path)
- **루트(`/`)에서부터 시작해서 목적지까지 전체 경로를 다 적는 방식**입니다.
- 예: `/Users/ellysuh/Documents/1-homework/mission.txt`
- 특징: 내가 지금 어느 폴더에 있든 상관없이 **항상 같은 곳**을 가리킵니다. (위치에 의존하지 않음)
- 비유: "대한민국 서울시 강남구 테헤란로 123" 처럼, 어디서 출발하든 유일하게 정해지는 주소.

### 상대 경로 (Relative Path)
- **현재 내가 있는 위치(현재 디렉토리)를 기준**으로 목적지를 표현하는 방식입니다.
- 기호:
  - `.` = 현재 폴더
  - `..` = 한 단계 위(부모) 폴더
- 예: 현재 위치가 `/Users/ellysuh/Documents` 라면
  - `1-homework/mission.txt` → `/Users/ellysuh/Documents/1-homework/mission.txt` 와 같은 곳
  - `../Desktop` → `/Users/ellysuh/Desktop`
- 특징: **내가 서 있는 위치가 바뀌면 같은 상대경로라도 가리키는 곳이 달라집니다.**
- 비유: "여기서 두 블록 직진, 오른쪽" 처럼 출발점에 따라 도착지가 달라지는 길 안내.

### 둘의 차이 요약
| 구분 | 절대 경로 | 상대 경로 |
|---|---|---|
| 시작점 | 항상 `/` (루트) | 현재 있는 위치 |
| 위치 의존성 | 없음 (어디서 실행해도 동일) | 있음 (실행 위치 따라 달라짐) |
| 예시 | `/Users/ellysuh/Documents/1-homework` | `../1-homework` 또는 `./mission.txt` |
| 장점 | 명확하고 안전함 | 짧고 편리함 |
| 주의점 | 경로가 길어짐, 컴퓨터마다 홈 경로가 다를 수 있음 | 현재 위치를 착각하면 엉뚱한 파일을 건드릴 위험 |

---

## 4. 파일 권한 개념

### 왜 권한이 필요한가?
여러 사람이 같은 컴퓨터/서버를 쓸 때, "누가 이 파일을 읽을 수 있는지, 고칠 수 있는지, 실행할 수 있는지"를 통제하지 않으면
실수로 또는 악의적으로 중요한 파일이 삭제되거나 변조될 수 있습니다. 그래서 리눅스는 파일마다 권한을 붙여둡니다.

### r / w / x 의 의미
| 기호 | 이름 | 의미 (파일 기준) | 의미 (디렉토리 기준) |
|---|---|---|---|
| r | read (읽기) | 파일 내용을 볼 수 있음 | 폴더 안의 목록을 볼 수 있음 (`ls`) |
| w | write (쓰기) | 파일 내용을 수정/삭제할 수 있음 | 폴더 안에 파일 생성/삭제 가능 |
| x | execute (실행) | 파일을 프로그램처럼 실행 가능 | 폴더 "안으로" 들어갈 수 있음 (`cd`) |

### 권한 대상 3그룹
리눅스는 권한을 "누구에게 적용할지" 3그룹으로 나눕니다.
1. **소유자(user, u)**: 그 파일을 만든/소유한 계정
2. **그룹(group, g)**: 소유자가 속한 그룹의 다른 사용자들
3. **기타(other, o)**: 그 외 모든 사람

### `ls -l` 출력 읽는 법
```
-rwxr-xr-x  1 ellysuh  staff   1234  7 30 20:00  app.sh
```
앞의 10글자를 이렇게 나눠서 읽습니다.

```
-   rwx    r-x    r-x
│    │      │      │
파일종류  소유자  그룹  기타
(- 파일 / d 디렉토리)
```
- 소유자(rwx): 읽기O 쓰기O 실행O
- 그룹(r-x): 읽기O 쓰기X 실행O
- 기타(r-x): 읽기O 쓰기X 실행O

### 숫자(8진수) 표기법 — 755, 644가 뭔가요?
r, w, x 각각에 숫자를 부여합니다: **r=4, w=2, x=1** (부여된 것만 더함)

| 조합 | 계산 | 숫자 |
|---|---|---|
| rwx | 4+2+1 | 7 |
| rw- | 4+2+0 | 6 |
| r-x | 4+0+1 | 5 |
| r-- | 4+0+0 | 4 |
| --- | 0 | 0 |

그래서 세 그룹(소유자/그룹/기타)의 숫자를 나란히 쓴 것이 755, 644 같은 표기입니다.

- **755** = `rwx r-x r-x` = 소유자는 다 가능(읽기/쓰기/실행), 그룹과 기타는 읽기+실행만 가능, 쓰기는 불가
  → 보통 **실행 파일이나 폴더**에 많이 씀 (다른 사람도 실행/진입은 하되, 수정은 못하게)
- **644** = `rw- r-- r--` = 소유자만 쓰기 가능, 다른 사람은 읽기만 가능
  → 보통 **일반 문서/설정 파일**에 많이 씀

### 권한 변경/확인 명령어
```bash
ls -l 파일명           # 현재 권한 확인
chmod 755 폴더명       # 권한을 755로 변경
chmod 644 파일명       # 권한을 644로 변경
chmod +x 파일명        # 실행 권한만 추가
chown 사용자:그룹 파일명  # 소유자 변경 (관리자 권한 필요, 서울캠퍼스 환경은 제한될 수 있음)
```

---

## 5. 터미널 기본 명령어 사전

| 명령어 | 뜻(원어) | 하는 일 | 예시 |
|---|---|---|---|
| `pwd` | print working directory | 현재 내가 있는 위치(절대경로)를 출력 | `pwd` |
| `ls` | list | 현재 폴더의 파일/폴더 목록 출력 | `ls -la` (숨김파일까지, 자세히) |
| `cd` | change directory | 다른 폴더로 이동 | `cd ~/Documents`, `cd ..` |
| `mkdir` | make directory | 새 폴더 생성 | `mkdir -p a/b/c` (-p: 중간 경로 없어도 한번에 생성) |
| `touch` | (터치) | 빈 파일 생성 (이미 있으면 수정시간만 갱신) | `touch app.py` |
| `cp` | copy | 파일/폴더 복사 | `cp a.txt b.txt`, `cp -r 폴더1 폴더2` |
| `mv` | move | 파일 이동 또는 **이름 변경** (같은 위치로 옮기면 이름변경 효과) | `mv old.txt new.txt` |
| `rm` | remove | 파일/폴더 삭제 (휴지통 없이 영구 삭제이므로 주의) | `rm a.txt`, `rm -r 폴더명` |
| `cat` | concatenate | 파일 내용을 화면에 전체 출력 | `cat README.md` |
| `less` | | 파일을 페이지 단위로 나눠서 보기(긴 파일용) | `less big.log` |
| `echo` | | 문자열/변수 값을 화면에 출력 | `echo "hi" > file.txt` |

### 숨김 파일이란?
- 파일/폴더 이름이 **`.`(마침표)로 시작**하면 "숨김 파일"로 취급되어 기본 `ls`에는 보이지 않습니다.
- 예: `.git`, `.gitignore`, `.zshrc` 등 (주로 설정 파일들)
- 숨김 파일까지 보려면: `ls -a` 또는 `ls -la`(자세히 + 숨김파일)

---

## 6. Docker 핵심 개념

### 문제 상황: 왜 Docker가 필요한가?
"내 컴퓨터에서는 되는데 서버에서는 안 돼요" 문제의 원인은 대부분:
- OS 버전이 다름
- 설치된 프로그램(Python, Node 등) 버전이 다름
- 환경변수/설정 파일이 다름

Docker는 **"프로그램 + 그 프로그램이 필요로 하는 모든 환경(라이브러리, 설정, 실행 방법)을 하나의 상자에 통째로 포장"**해서,
어떤 컴퓨터에서 실행하든 **완전히 동일하게 작동**하도록 만들어주는 기술입니다.

### 가상머신(VM)과 다른 점 (간단 비교)
| 구분 | 가상머신(VM) | Docker 컨테이너 |
|---|---|---|
| 비유 | 아파트를 통째로 새로 짓는 것 | 한 건물 안에 칸막이로 방을 나누는 것 |
| 내부 OS | 각자 완전한 별도 OS를 가짐 (무거움) | 호스트의 OS 커널을 공유 (가벼움) |
| 실행 속도 | 느림(수십 초~분) | 빠름(초 단위) |
| 자원 사용 | 많음 | 적음 |

### 이미지(Image) vs 컨테이너(Container) — 가장 중요한 개념
이 미션에서 계속 나오는 "이미지와 컨테이너의 분리"가 바로 이 개념입니다.

- **이미지(Image)** = 실행에 필요한 모든 것(프로그램, 설정, 파일)이 담긴 **읽기 전용 설계도/틀**
- **컨테이너(Container)** = 그 이미지를 바탕으로 **실제로 실행되고 있는 인스턴스(실행 중인 상태)**

**비유: 붕어빵**
- 붕어빵 틀 = 이미지 (하나의 틀로 몇 개든 똑같은 붕어빵을 찍어낼 수 있음)
- 붕어빵 = 컨테이너 (틀에서 찍혀 나온, 실제로 존재하는 개별 붕어빵)
- 같은 틀(이미지)로 여러 개의 붕어빵(컨테이너)을 동시에 만들 수 있고, 하나를 먹어도(삭제해도) 틀은 그대로 남습니다.

**비유2: 클래스와 인스턴스(프로그래밍 아는 사람이라면)**
- 이미지 = 클래스(설계도), 컨테이너 = 그 클래스로 만든 객체(실제 동작하는 것)

### Docker 데몬(daemon)이란?
- **데몬**은 사용자가 직접 실행하지 않아도 백그라운드에서 계속 대기하며 작업을 처리해주는 프로그램을 말합니다.
- **Docker 데몬**은 실제로 이미지를 빌드하고, 컨테이너를 실행/관리하는 "엔진(핵심 작업자)"입니다.
- 우리가 터미널에 치는 `docker` 명령어는 사실 이 데몬에게 "이렇게 해줘"라고 요청을 보내는 역할만 하고,
  진짜 작업은 데몬이 처리합니다. (그래서 `docker info`로 데몬이 켜져 있는지 확인하는 것)

### 격리(Isolation)된 실행 환경이란?
- 각 컨테이너는 마치 "독립된 작은 컴퓨터"처럼 자기만의 파일시스템, 네트워크, 프로세스 공간을 가진 것처럼 동작합니다.
- 그래서 컨테이너 A에서 무슨 일이 생겨도(파일 삭제, 프로그램 충돌 등) 컨테이너 B나 호스트(내 실제 컴퓨터)에는 영향을 주지 않습니다.
- 이 격리 덕분에 "같은 서비스를 여러 번 실행해도 항상 동일하게 재현"되는 것이 가능해집니다. (미션의 핵심 목표 문장과 연결됨)

### OrbStack이란? (서울캠퍼스 관련 핵심 개념)
- macOS는 원래 리눅스 커널이 없기 때문에, Docker(원래 리눅스 기술)를 macOS에서 쓰려면 **내부적으로 아주 작은 리눅스 가상머신**을 하나 띄워서 그 안에서 Docker 엔진을 돌립니다.
- **Docker Desktop**과 **OrbStack**은 둘 다 "macOS에서 Docker를 쉽게 쓸 수 있게 해주는 애플리케이션"입니다. 즉 둘 다 위 문제를 해결해주는 도구입니다.
- OrbStack이 필요한 이유(서울캠퍼스): 일부 환경은 보안 정책상 `sudo`(관리자 권한) 사용이 제한되어 Docker를 직접 설치/제어하기 어렵습니다. OrbStack은 앱을 실행하기만 하면 sudo 없이도 내부적으로 Docker 엔진을 띄워주기 때문에 이 제약을 우회할 수 있습니다.
- 사용법: OrbStack 앱을 실행 → 터미널에서는 평소처럼 `docker run`, `docker ps`, `docker build` 등을 그대로 사용하면 됩니다. (명령어 자체는 동일함, 뒷단에서 OrbStack이 처리)

### sudo란?
- **sudo (superuser do)** = "관리자 권한으로 이 명령을 실행해줘"라는 뜻의 명령어입니다.
- 시스템 설정 변경, 프로그램 설치 등 민감한 작업은 보통 sudo가 필요한데, 보안 정책상 이 권한이 제한된 환경(서울캠퍼스)에서는 sudo 없이 동작하는 대안(OrbStack)을 씁니다.

---

## 7. Docker 명령어 사전

| 명령어 | 하는 일 | 자주 쓰는 옵션 |
|---|---|---|
| `docker --version` | 설치된 Docker 버전 확인 | |
| `docker info` | Docker 데몬(엔진)이 정상 동작 중인지, 시스템 정보 확인 | |
| `docker images` | 로컬에 다운로드/빌드된 이미지 목록 확인 | |
| `docker pull <이미지>` | Docker Hub(이미지 저장소)에서 이미지를 다운로드 | |
| `docker run <이미지>` | 이미지를 바탕으로 새 컨테이너를 만들어 실행 | `-d`(백그라운드), `-it`(터미널 대화형 접속), `-p`(포트매핑), `-v`(볼륨/마운트), `--name`(이름 지정) |
| `docker ps` | **실행 중인** 컨테이너 목록 확인 | `-a`(중지된 것까지 전체) |
| `docker exec -it <컨테이너> bash` | **이미 실행 중인** 컨테이너 내부에 새 터미널로 진입 | |
| `docker attach <컨테이너>` | 실행 중인 컨테이너의 "메인 프로세스"에 직접 접속(연결) | |
| `docker logs <컨테이너>` | 컨테이너가 출력한 로그(기록) 확인 | `-f`(실시간 따라가기) |
| `docker stats` | 실행 중인 컨테이너들의 CPU/메모리 사용량 실시간 확인 | |
| `docker stop <컨테이너>` | 실행 중인 컨테이너를 정상 종료 | |
| `docker rm <컨테이너>` | 컨테이너 삭제 (실행 중이면 실패) | `-f`(강제로 실행 중이어도 삭제) |
| `docker rmi <이미지>` | 이미지 삭제 | |
| `docker build -t 이름:태그 .` | 현재 폴더의 Dockerfile을 읽어서 이미지를 빌드(생성) | `-t`(태그=이름 붙이기), `.`(빌드 재료가 있는 현재 경로) |
| `docker volume create <이름>` | 데이터를 영구 보관할 볼륨 생성 | |
| `docker volume ls` | 볼륨 목록 확인 | |

### `docker run` vs `docker exec` vs `docker attach` 차이 (미션에서 직접 관찰하라고 한 부분)
| 명령어 | 상황 | 결과 |
|---|---|---|
| `docker run` | 컨테이너가 **아직 없을 때** | 이미지로부터 **새 컨테이너를 생성**해서 실행 |
| `docker exec -it` | 컨테이너가 **이미 실행 중**일 때 | 그 컨테이너 안에 **새로운 별도의 터미널 세션**을 하나 더 엶 (기존 접속은 안 끊김) |
| `docker attach` | 컨테이너가 **이미 실행 중**일 때 | 컨테이너의 **원래 메인 프로세스 화면에 그대로 연결**(같은 세션을 보는 것이라 여기서 나가면(Ctrl+C 등) 컨테이너 자체가 멈출 수 있음) |

비유: `exec`는 "그 집에 새로운 문을 하나 더 만들어서 들어가는 것", `attach`는 "이미 열려 있는 문으로 들어가서 그 안 사람과 같은 화면을 보는 것"

### hello-world 컨테이너와 ubuntu 컨테이너
- `docker run hello-world`: Docker가 정상적으로 이미지를 받아오고, 컨테이너를 실행하고, 메시지를 출력한 뒤 종료하는지 확인하는 **가장 기본적인 테스트용 이미지**입니다. (마치 "Hello World" 프로그램처럼 설치 확인용)
- `docker run -it ubuntu bash`: `ubuntu`라는 리눅스 배포판이 담긴 이미지를 기반으로 컨테이너를 실행하고, 그 안에서 bash 쉘로 직접 명령어(`ls`, `echo` 등)를 입력해볼 수 있게 해줍니다. "리눅스 컴퓨터 한 대를 즉석에서 빌려 쓰는 것"이라고 생각하면 됩니다.

---

## 8. Dockerfile 개념

### Dockerfile이란?
- **이미지를 어떻게 만들지 순서대로 적어놓은 "레시피 파일"** 입니다.
- 이 파일을 `docker build` 명령으로 읽으면, 그 안의 명령을 한 줄씩 실행해서 최종 이미지를 만들어줍니다.
- 확장자 없이 파일명 자체가 `Dockerfile` 입니다.

### 자주 쓰는 Dockerfile 명령어(instruction)
| 명령어 | 뜻 | 예시 |
|---|---|---|
| `FROM` | 어떤 이미지를 기반(베이스)으로 시작할지 지정 (항상 첫 줄) | `FROM nginx:alpine` |
| `LABEL` | 이미지에 메타데이터(설명, 제목 등)를 붙임 | `LABEL org.opencontainers.image.title="my-app"` |
| `ENV` | 컨테이너 내부에서 사용할 환경변수 지정 | `ENV APP_ENV=dev` |
| `COPY` | 호스트(내 컴퓨터)의 파일을 이미지 안으로 복사 | `COPY site/ /usr/share/nginx/html/` |
| `RUN` | 이미지를 만드는 **빌드 시점**에 실행할 명령 (패키지 설치 등) | `RUN apt-get update && apt-get install -y curl` |
| `USER` | 이후 명령을 어떤 사용자 권한으로 실행할지 지정 | `USER appuser` |
| `HEALTHCHECK` | 컨테이너가 "정상 동작 중"인지 주기적으로 확인하는 방법 지정 | `HEALTHCHECK CMD curl -f http://localhost/ || exit 1` |
| `CMD` / `ENTRYPOINT` | 컨테이너가 **실행될 때** 기본으로 실행할 명령 지정 | `CMD ["nginx", "-g", "daemon off;"]` |
| `EXPOSE` | 이 컨테이너가 몇 번 포트를 쓰는지 문서화(실제 매핑은 `-p`가 함) | `EXPOSE 80` |

### "베이스 이미지"란?
- 처음부터 모든 걸 직접 만들지 않고, 이미 만들어져 있는 이미지를 시작점(FROM)으로 삼는 것입니다.
- 예: `nginx:alpine`(가벼운 리눅스+nginx 웹서버가 이미 설치된 이미지), `ubuntu:22.04`(우분투 리눅스만 깔린 기본 이미지)
- 미션에서 말하는 "커스텀 이미지"란 이런 기존 베이스 이미지 위에 내 설정/코드를 얹어서 "나만의 이미지"로 만드는 것을 뜻합니다.

### 빌드(build)와 실행(run)의 관계
```
Dockerfile  --(docker build)-->  이미지  --(docker run)-->  컨테이너
   (레시피)                      (틀)                      (실제 실행중인 것)
```

---

## 9. 포트 매핑

### 포트(Port)란?
- 컴퓨터 한 대 안에서 여러 프로그램이 동시에 네트워크 통신을 할 수 있도록, 각 프로그램마다 붙는 "문 번호"라고 생각하면 됩니다.
- 예: 웹서버는 보통 80번, HTTPS는 443번 포트를 씁니다.

### 왜 포트 매핑이 필요한가?
- 컨테이너는 "격리된 환경"이기 때문에, 컨테이너 내부의 포트(예: 컨테이너 안 웹서버가 쓰는 80번)는 기본적으로 **내 컴퓨터(호스트) 바깥에서 보이지 않습니다.**
- **포트 매핑**은 "내 컴퓨터의 특정 포트로 들어온 요청을, 컨테이너 내부의 특정 포트로 연결해주는 다리"입니다.
- 이 다리를 놓아야만 브라우저에서 `localhost:8080` 같은 주소로 컨테이너 안의 서비스에 접속할 수 있습니다.

### 문법
```bash
docker run -p <호스트포트>:<컨테이너포트> 이미지이름
docker run -d -p 8080:80 --name my-web my-web:1.0
```
- 위 예시는 "내 컴퓨터의 8080번 포트로 오는 요청을, 컨테이너 내부의 80번 포트로 넘겨준다"는 뜻입니다.
- 그래서 브라우저에 `http://localhost:8080` 이라고 치면, 실제로는 컨테이너 안의 80번 포트에서 응답하는 웹서버 내용이 보이게 됩니다.
- 같은 이미지로 컨테이너를 여러 개 띄울 때는, 호스트 포트를 서로 다르게(8080, 8081 등) 지정해야 충돌이 나지 않습니다. (같은 호스트 포트를 두 번 쓸 수 없기 때문)

---

## 10. 바인드 마운트 vs 볼륨(데이터 영속성)

### 배경: 컨테이너 내부의 데이터는 원래 "휘발성"
- 컨테이너를 삭제하면, 그 안에 저장했던 파일들도 기본적으로 **모두 함께 사라집니다.**
- 이것을 해결하려는 두 가지 방법이 "바인드 마운트"와 "볼륨"입니다. 둘 다 "컨테이너 바깥"에 데이터를 두고 컨테이너와 연결하는 방식입니다.

### 바인드 마운트(Bind Mount)
- **내 컴퓨터(호스트)의 특정 폴더**를 컨테이너 내부의 특정 폴더에 그대로 연결하는 것입니다.
- 문법: `-v 호스트경로:컨테이너경로`
- 특징: 호스트에서 파일을 수정하면 **컨테이너 안에도 실시간으로 즉시 반영**됩니다. (반대도 마찬가지)
- 용도: 개발 중에 코드를 수정할 때마다 다시 빌드하지 않고 바로 반영해서 확인하고 싶을 때 주로 사용합니다.
```bash
docker run -d -p 8082:80 -v $(pwd)/site:/usr/share/nginx/html nginx:alpine
```
(내 컴퓨터의 `./site` 폴더 = 컨테이너 안의 `/usr/share/nginx/html` 폴더, 완전히 같은 내용을 공유)

### 볼륨(Docker Volume)
- 호스트의 "아무 폴더"를 지정하는 바인드 마운트와 달리, **Docker가 자체적으로 관리하는 전용 저장 공간**을 만들어서 연결하는 방식입니다.
- 문법: 먼저 `docker volume create 이름` 으로 생성 → `-v 볼륨이름:컨테이너경로` 로 연결
- 특징: 컨테이너를 삭제해도 볼륨 자체는 사라지지 않고 남아있기 때문에, 새 컨테이너를 같은 볼륨에 다시 연결하면 이전 데이터를 그대로 이어받을 수 있습니다. 이것이 **"데이터 영속성(Persistence)"** 입니다.
```bash
docker volume create mydata
docker run -d --name c1 -v mydata:/data ubuntu sleep infinity
docker exec c1 bash -c "echo hi > /data/hello.txt"
docker rm -f c1                      # 컨테이너는 삭제됨
docker run -d --name c2 -v mydata:/data ubuntu sleep infinity
docker exec c2 cat /data/hello.txt   # 그래도 데이터(hi)는 살아있음!
```

### 바인드 마운트 vs 볼륨 비교
| 구분 | 바인드 마운트 | 볼륨 |
|---|---|---|
| 연결 대상 | 호스트의 임의 폴더 (내가 지정) | Docker가 관리하는 전용 저장소 |
| 주 용도 | 개발 중 코드 변경 실시간 반영 | 운영 중 데이터(DB 등) 영구 보관 |
| 관리 주체 | 사용자(내가 직접 경로 관리) | Docker |
| 이식성 | 호스트 경로에 의존 (다른 컴퓨터에서 경로 다르면 문제) | Docker 어디서든 이름으로 관리, 더 이식성 좋음 |

---

## 11. Docker Compose (보너스 관련)

### Docker Compose란?
- 컨테이너를 하나씩 `docker run ... -p ... -v ... --name ...` 처럼 긴 명령어로 실행하는 대신,
  **모든 실행 설정을 `docker-compose.yml`이라는 하나의 문서 파일로 정리**해두고, 명령 한 줄로 실행/종료하게 해주는 도구입니다.
- 미션에서 강조하는 배움 포인트: "실행 명령이 문서화된 실행 설정으로 바뀐다" = 매번 긴 명령어를 외워서 치는 대신, 파일 하나만 보면 "이 서비스가 어떻게 실행되는지" 누구나 알 수 있게 된다는 뜻입니다.

### docker-compose.yml 기본 구조
```yaml
services:          # 실행할 컨테이너(서비스)들을 정의하는 영역
  web:              # 서비스 이름 (마음대로 지정 가능)
    build: .        # 현재 폴더의 Dockerfile로 이미지를 빌드해서 사용
    ports:
      - "8080:80"   # docker run의 -p 8080:80 과 동일
    environment:
      - PORT=3000   # docker run의 -e PORT=3000 과 동일 (환경변수 주입)
    depends_on:
      - redis       # redis 서비스가 먼저 뜬 뒤 실행되도록 순서 지정
  redis:
    image: redis:alpine   # 이미 만들어진 이미지를 그대로 사용(빌드 X)
```

### 멀티 컨테이너 & 서비스 디스커버리(Service Discovery)
- 하나의 `docker-compose.yml`에 `web`, `redis`처럼 **여러 서비스**를 함께 정의하면, Compose가 자동으로 이들을 위한 **전용 가상 네트워크**를 만들어줍니다.
- 이 네트워크 안에서는 컨테이너들이 IP주소를 몰라도 **서비스 이름 자체를 마치 도메인 주소처럼 사용**해서 서로를 찾아갈 수 있습니다. (예: `web` 컨테이너 안에서 `ping redis` 하면 자동으로 연결됨)
- 이렇게 "이름만으로 서로를 자동으로 찾아내는 것"을 **서비스 디스커버리**라고 합니다.

### Compose 운영 명령어
| 명령어 | 하는 일 |
|---|---|
| `docker compose up -d` | yml에 정의된 모든 서비스를 백그라운드로 실행 |
| `docker compose down` | 실행 중인 서비스들을 전부 중지 및 삭제 |
| `docker compose ps` | Compose로 띄운 서비스들의 상태 확인 |
| `docker compose logs` | 서비스들의 로그 확인 |
| `docker compose exec 서비스명 명령` | 실행 중인 특정 서비스 컨테이너 안에서 명령 실행 |

### 환경 변수와 "설정과 코드의 분리"
- 코드 안에 `PORT = 5000` 처럼 값을 직접 박아 넣으면(하드코딩), 값을 바꿀 때마다 코드를 고치고 다시 빌드해야 합니다.
- 대신 **환경변수(ENV/environment)** 로 외부에서 값을 주입하면, 코드는 그대로 두고 실행할 때마다 다른 설정(포트, 모드 등)을 적용할 수 있습니다.
- 이것이 "설정(configuration)과 코드(code)를 분리한다"는 원칙이며, 실무에서 매우 중요하게 여겨지는 습관입니다.

---

## 12. Git 개념

### Git이란?
- **버전 관리 시스템(Version Control System)**: 파일이 변경된 "역사(히스토리)"를 기록해서, 언제든 예전 상태로 되돌리거나 누가 무엇을 바꿨는지 추적할 수 있게 해주는 도구입니다.
- 비유: 문서 작업할 때 "저장1, 저장2, 최종, 최종진짜최종.docx" 처럼 파일을 여러 개 만드는 대신, Git은 "하나의 파일에서 모든 변경 이력을 자동으로, 체계적으로" 저장해줍니다.
- Git은 **내 컴퓨터(로컬)에서 동작하는 프로그램**입니다. 인터넷 연결이 없어도 사용할 수 있습니다.

### 핵심 개념
| 용어 | 뜻 |
|---|---|
| Repository (저장소, repo) | Git이 버전을 관리하는 프로젝트 폴더 전체 |
| Commit | 특정 시점의 변경사항을 하나의 "스냅샷"으로 저장하는 행위/단위 |
| Branch (브랜치) | 메인 흐름에서 갈라져 나와 독립적으로 작업할 수 있는 "가지" (기본 브랜치는 보통 `main`) |
| Staging Area | 커밋하기 전에 "이번에 저장할 변경사항"을 미리 담아두는 공간 (`git add`로 담음) |

### 기본 흐름
```bash
git init                                  # 현재 폴더를 Git 저장소로 초기화
git config --global user.name "이름"      # 커밋에 남길 내 이름 설정
git config --global user.email "메일"     # 커밋에 남길 내 이메일 설정
git config --global init.defaultBranch main  # 새 저장소의 기본 브랜치 이름을 main으로 설정
git config --list                         # 현재 적용된 설정값 전체 확인

git add 파일명                             # 변경사항을 커밋 준비 영역(staging)에 추가
git commit -m "메시지"                     # 준비된 변경사항을 하나의 버전으로 저장
```
- `--global` 옵션은 "이 컴퓨터의 모든 Git 저장소에 공통 적용"이라는 뜻입니다. (저장소별로 다르게 하려면 `--global` 빼고 그 저장소 안에서 설정)

---

## 13. GitHub 개념 및 Git과의 차이

### GitHub이란?
- Git으로 관리하는 저장소를 **인터넷(클라우드)에 올려서, 여러 사람과 함께 보고 협업할 수 있게 해주는 웹 서비스(플랫폼)** 입니다.
- Git 저장소를 백업해두는 곳이자, 팀원들과 코드를 주고받는 "공용 창고"라고 생각하면 됩니다.

### Git vs GitHub — 가장 헷갈리는 부분
| 구분 | Git | GitHub |
|---|---|---|
| 정체 | 프로그램(도구) | 웹 서비스(플랫폼) |
| 동작 위치 | 내 컴퓨터(로컬) | 인터넷 서버(원격) |
| 역할 | 버전을 기록/관리 | 저장소를 온라인에 올려 공유·협업 |
| 인터넷 필요 여부 | 필요 없음 | 필요함 |
| 비유 | "한글/워드의 변경 이력 추적 기능" | "그 문서를 올려서 팀원과 공유하는 구글 드라이브" |

즉, **Git은 로컬 버전관리**, **GitHub은 그것을 온라인에서 공유/협업하게 해주는 원격 플랫폼**입니다. Git 없이 GitHub만 쓸 수 없고(둘은 함께 쓰는 것), GitHub 없이 Git만 쓸 수도 있습니다(그냥 내 컴퓨터에서만 버전관리).

### 연동 관련 명령/개념
```bash
git remote add origin <저장소 URL>   # 내 로컬 저장소와 GitHub 원격 저장소를 연결
git push origin main                 # 로컬의 커밋들을 GitHub(원격)으로 업로드
git pull                             # GitHub의 최신 내용을 내 로컬로 가져오기
```
- **VSCode-GitHub 연동**: VSCode 안에서 GitHub 계정으로 로그인하면, 별도로 터미널에 아이디/비번을 치지 않아도 클릭만으로 커밋/푸시/저장소 열기 등을 할 수 있게 해주는 기능입니다.

---

## 14. SSH vs HTTPS 인증 (보너스 관련)

### 배경: GitHub에 push하려면 "내가 이 계정의 주인"임을 증명해야 함
GitHub은 아무나 저장소에 코드를 올리지 못하도록, 매번 신원 확인(인증)을 요구합니다. 방법이 두 가지 있습니다.

### HTTPS 방식
- 저장소 주소가 `https://github.com/...` 형태
- 인증 시 보통 GitHub 계정의 **개인 접근 토큰(Personal Access Token)**을 비밀번호 대신 입력
- 장점: 설정이 간단함 / 단점: 토큰을 주기적으로 관리(재발급)해야 하고, 실수로 노출되면 위험함

### SSH 방식
- 저장소 주소가 `git@github.com:...` 형태
- **공개키(public key) / 개인키(private key)** 한 쌍을 미리 만들어서, 공개키는 GitHub에 등록하고 개인키는 내 컴퓨터에만 보관
- 접속할 때마다 비밀번호/토큰을 입력하지 않아도, 내 컴퓨터에 있는 개인키로 "내가 맞다"는 것을 암호학적으로 증명함
- 장점: 한번 등록하면 이후 계속 편함, 토큰 노출 위험이 상대적으로 적음(개인키는 파일 밖으로 절대 유출되면 안 됨)

```bash
ssh-keygen -t ed25519 -C "이메일"     # 키 쌍(공개키+개인키) 생성
cat ~/.ssh/id_ed25519.pub             # 공개키 내용 확인 (이것만 GitHub에 등록)
ssh -T git@github.com                 # GitHub과 SSH 연결이 잘 되는지 테스트
```
> 주의: `id_ed25519` (확장자 없는 파일, 개인키)는 **절대 공유/캡처하면 안 됩니다.** GitHub에 등록하는 것은 `.pub`가 붙은 **공개키**뿐입니다.

---

## 15. 기술 문서 / README 관련 개념

### README.md란?
- 저장소에 들어갔을 때 **가장 먼저, 자동으로 보여지는 설명 문서**입니다. (GitHub이 자동으로 README.md를 저장소 첫 화면에 렌더링해서 보여줌)
- `.md`는 **마크다운(Markdown)** 이라는 문서 작성 문법의 확장자입니다. `#`으로 제목을, `-`로 목록을, 백틱(`)으로 코드 표시를 하는 식의 간단한 규칙으로 글을 꾸밀 수 있습니다.

### 왜 README가 "기술 문서"로서 중요한가
- 이 미션의 평가 기준 자체가 "README만 보고도 평가자가 전체 수행 내용을 파악하고 재현할 수 있어야 한다"는 것입니다.
- 즉 README는 단순 설명이 아니라, **내가 한 작업을 남이 검증할 수 있게 만드는 증거 문서**의 역할을 합니다.

### 재현성(Reproducibility)이란?
- "내가 한 작업을, 다른 사람이 같은 문서만 보고 따라 해도 똑같은 결과가 나오는 성질"을 뜻합니다.
- 미션 전체를 관통하는 목표: 개발 환경을 세팅하는 방법 자체를 "문서화 + 자동화(Dockerfile, Compose)"해서, 누가 실행해도 같은 결과가 나오게 만드는 것.

---

## 16. 보안·개인정보 관련 개념

### 왜 민감정보를 마스킹해야 하는가?
- 토큰(Token), 비밀번호, 개인키, 인증 코드 등이 스크린샷/로그/커밋 기록에 남으면, 그 정보를 본 **누구나 내 GitHub 계정이나 서비스에 접근**할 수 있게 됩니다.
- 특히 GitHub는 공개 저장소면 전 세계 누구나 볼 수 있고, 한번 커밋 이력에 올라가면 나중에 지워도 **과거 커밋 기록에는 남아있을 수 있어서** 각별히 주의해야 합니다.

### 마스킹(Masking)이란?
- 민감한 부분을 `****` 처럼 가리거나, 스크린샷 캡처 시 해당 부분을 잘라내거나 모자이크 처리하는 것을 말합니다.

### 만약 노출되었다면?
- 즉시 해당 토큰/비밀번호를 **폐기하고 재발급**해야 합니다. (그 값 자체를 이미 알아버린 사람이 있을 수 있으므로, 값을 숨기는 것만으론 늦음 → 값 자체를 무효화해야 안전)

---

## 17. 트러블슈팅 기록법

미션에서 요구하는 형식은 다음 4단계입니다.

1. **문제(Problem)**: 무슨 에러/이상 현상이 발생했는가 (에러 메시지 그대로 인용)
2. **원인 가설(Hypothesis)**: 왜 이런 문제가 생겼을지 내가 추측하는 이유
3. **확인(Verify)**: 그 가설이 맞는지 어떤 명령/방법으로 검증했는가
4. **해결/대안(Fix)**: 실제로 어떻게 고쳤는지, 혹은 완전히 해결 못했다면 어떤 대안을 썼는지

이 형식이 중요한 이유: 단순히 "안 되던 게 됐어요"가 아니라, **문제 해결 과정 자체를 논리적으로 설명할 수 있는 능력**을 보여주기 위함입니다. 실무에서도 장애 보고서(포스트모템)는 거의 이 구조를 따릅니다.

---

## 18. 용어 요약표 (초압축 버전)

| 용어 | 한 줄 요약 |
|---|---|
| 터미널 | 글자 명령으로 컴퓨터를 조작하는 창 |
| 쉘(bash/zsh) | 터미널 안에서 명령을 해석/처리하는 프로그램 |
| 절대경로 | `/`부터 시작하는, 위치와 무관하게 항상 같은 주소 |
| 상대경로 | 현재 위치 기준으로 표현하는 주소 (`.`, `..`) |
| 권한(r/w/x) | 읽기/쓰기/실행 가능 여부 |
| 755 / 644 | 소유자/그룹/기타 각각의 권한을 숫자로 표기한 것 |
| Docker | 실행 환경을 통째로 포장해서 어디서든 동일하게 실행하는 기술 |
| 이미지 | 실행 환경의 설계도(틀), 읽기 전용 |
| 컨테이너 | 이미지로부터 실제로 실행 중인 것 (붕어빵 틀 vs 붕어빵) |
| Docker 데몬 | 실제로 이미지 빌드/컨테이너 실행을 처리하는 백그라운드 엔진 |
| OrbStack | macOS에서 sudo 없이 Docker 엔진을 돌릴 수 있게 해주는 앱 |
| Dockerfile | 이미지를 만드는 절차가 적힌 레시피 파일 |
| 포트 매핑 | 호스트 포트 ↔ 컨테이너 포트를 연결하는 다리 (`-p host:container`) |
| 바인드 마운트 | 호스트 폴더를 컨테이너에 실시간 연결 (개발용) |
| 볼륨 | Docker가 관리하는 영구 저장 공간 (컨테이너 삭제해도 데이터 유지) |
| 격리 | 컨테이너끼리 서로 영향 주지 않고 독립적으로 동작하는 성질 |
| Docker Compose | 여러 컨테이너 실행 설정을 yml 파일 하나로 문서화/관리 |
| 서비스 디스커버리 | Compose 네트워크 안에서 서비스 이름으로 서로를 찾는 것 |
| Git | 로컬에서 코드 변경 이력을 관리하는 도구 |
| GitHub | Git 저장소를 온라인에 올려 협업하는 플랫폼 |
| SSH 인증 | 키 쌍(공개키/개인키)으로 비밀번호 없이 안전하게 인증하는 방식 |
| README.md | 저장소 첫 화면에 자동 표시되는 설명/증거 문서 |
| 재현성 | 문서만 보고 남이 따라 해도 같은 결과가 나오는 성질 |
| 마스킹 | 토큰/비밀번호 등 민감정보를 가리는 것 |
